// @generated by `cargo xtask codegen` from protocol/protocol.json. Do not edit.

#![allow(
    clippy::large_enum_variant,
    clippy::redundant_field_names,
    clippy::struct_excessive_bools,
    clippy::too_many_arguments,
    rustdoc::bare_urls
)]

use alloc::string::{String, ToString};
use alloc::vec::Vec;

use super::*;

fn sample_scene() -> Scene {
    Scene {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        scene_index: None,
    }
}

#[test]
fn roundtrip_scene() {
    let value = sample_scene();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    let back: Scene = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_scene_item_transform() -> SceneItemTransform {
    SceneItemTransform {
        source_width: 0.0,
        source_height: 0.0,
        position_x: 0.0,
        position_y: 0.0,
        rotation: 0.0,
        scale_x: 0.0,
        scale_y: 0.0,
        width: 0.0,
        height: 0.0,
        alignment: 0,
        bounds_type: 0,
        bounds_alignment: 0,
        bounds_width: 0.0,
        bounds_height: 0.0,
        crop_left: 0,
        crop_right: 0,
        crop_top: 0,
        crop_bottom: 0,
        crop_to_bounds: false,
    }
}

#[test]
fn roundtrip_scene_item_transform() {
    let value = sample_scene_item_transform();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceWidth").is_some());
    assert!(json.get("sourceHeight").is_some());
    assert!(json.get("positionX").is_some());
    assert!(json.get("positionY").is_some());
    assert!(json.get("rotation").is_some());
    assert!(json.get("scaleX").is_some());
    assert!(json.get("scaleY").is_some());
    assert!(json.get("width").is_some());
    assert!(json.get("height").is_some());
    assert!(json.get("alignment").is_some());
    assert!(json.get("boundsType").is_some());
    assert!(json.get("boundsAlignment").is_some());
    assert!(json.get("boundsWidth").is_some());
    assert!(json.get("boundsHeight").is_some());
    assert!(json.get("cropLeft").is_some());
    assert!(json.get("cropRight").is_some());
    assert!(json.get("cropTop").is_some());
    assert!(json.get("cropBottom").is_some());
    assert!(json.get("cropToBounds").is_some());
    let back: SceneItemTransform = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_scene_item_transform_partial() -> SceneItemTransformPartial {
    SceneItemTransformPartial {
        source_width: None,
        source_height: None,
        position_x: None,
        position_y: None,
        rotation: None,
        scale_x: None,
        scale_y: None,
        width: None,
        height: None,
        alignment: None,
        bounds_type: None,
        bounds_alignment: None,
        bounds_width: None,
        bounds_height: None,
        crop_left: None,
        crop_right: None,
        crop_top: None,
        crop_bottom: None,
        crop_to_bounds: None,
    }
}

#[test]
fn roundtrip_scene_item_transform_partial() {
    let value = sample_scene_item_transform_partial();
    let json = serde_json::to_value(&value).unwrap();
    let back: SceneItemTransformPartial = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_scene_item() -> SceneItem {
    SceneItem {
        scene_item_id: 0,
        scene_item_index: 0,
        scene_item_enabled: false,
        scene_item_locked: false,
        scene_item_transform: sample_scene_item_transform(),
        scene_item_blend_mode: 0,
        source_name: "sample".to_string(),
        source_uuid: "sample".to_string(),
        source_type: 0,
        input_kind: None,
        is_group: None,
    }
}

#[test]
fn roundtrip_scene_item() {
    let value = sample_scene_item();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemIndex").is_some());
    assert!(json.get("sceneItemEnabled").is_some());
    assert!(json.get("sceneItemLocked").is_some());
    assert!(json.get("sceneItemTransform").is_some());
    assert!(json.get("sceneItemBlendMode").is_some());
    assert!(json.get("sourceName").is_some());
    assert!(json.get("sourceUuid").is_some());
    assert!(json.get("sourceType").is_some());
    let back: SceneItem = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_scene_item_index() -> SceneItemIndex {
    SceneItemIndex {
        scene_item_id: 0,
        scene_item_index: 0,
    }
}

#[test]
fn roundtrip_scene_item_index() {
    let value = sample_scene_item_index();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemIndex").is_some());
    let back: SceneItemIndex = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_input() -> Input {
    Input {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_kind: "sample".to_string(),
        unversioned_input_kind: "sample".to_string(),
        input_kind_caps: 0,
    }
}

#[test]
fn roundtrip_input() {
    let value = sample_input();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputKind").is_some());
    assert!(json.get("unversionedInputKind").is_some());
    assert!(json.get("inputKindCaps").is_some());
    let back: Input = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_source_filter() -> SourceFilter {
    SourceFilter {
        filter_enabled: false,
        filter_index: 0,
        filter_kind: "sample".to_string(),
        filter_name: "sample".to_string(),
        filter_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_source_filter() {
    let value = sample_source_filter();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterEnabled").is_some());
    assert!(json.get("filterIndex").is_some());
    assert!(json.get("filterKind").is_some());
    assert!(json.get("filterName").is_some());
    assert!(json.get("filterSettings").is_some());
    let back: SourceFilter = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_transition() -> Transition {
    Transition {
        transition_name: "sample".to_string(),
        transition_uuid: "sample".to_string(),
        transition_kind: "sample".to_string(),
        transition_fixed: false,
        transition_configurable: false,
    }
}

#[test]
fn roundtrip_transition() {
    let value = sample_transition();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionName").is_some());
    assert!(json.get("transitionUuid").is_some());
    assert!(json.get("transitionKind").is_some());
    assert!(json.get("transitionFixed").is_some());
    assert!(json.get("transitionConfigurable").is_some());
    let back: Transition = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_output_flags() -> OutputFlags {
    OutputFlags {
        audio: false,
        video: false,
        encoded: false,
        multi_track: false,
        service: false,
    }
}

#[test]
fn roundtrip_output_flags() {
    let value = sample_output_flags();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("OBS_OUTPUT_AUDIO").is_some());
    assert!(json.get("OBS_OUTPUT_VIDEO").is_some());
    assert!(json.get("OBS_OUTPUT_ENCODED").is_some());
    assert!(json.get("OBS_OUTPUT_MULTI_TRACK").is_some());
    assert!(json.get("OBS_OUTPUT_SERVICE").is_some());
    let back: OutputFlags = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_output() -> Output {
    Output {
        output_name: "sample".to_string(),
        output_kind: "sample".to_string(),
        output_width: 0,
        output_height: 0,
        output_active: false,
        output_flags: sample_output_flags(),
    }
}

#[test]
fn roundtrip_output() {
    let value = sample_output();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputName").is_some());
    assert!(json.get("outputKind").is_some());
    assert!(json.get("outputWidth").is_some());
    assert!(json.get("outputHeight").is_some());
    assert!(json.get("outputActive").is_some());
    assert!(json.get("outputFlags").is_some());
    let back: Output = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_monitor() -> Monitor {
    Monitor {
        monitor_name: "sample".to_string(),
        monitor_index: 0,
        monitor_width: 0,
        monitor_height: 0,
        monitor_position_x: 0,
        monitor_position_y: 0,
    }
}

#[test]
fn roundtrip_monitor() {
    let value = sample_monitor();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("monitorName").is_some());
    assert!(json.get("monitorIndex").is_some());
    assert!(json.get("monitorWidth").is_some());
    assert!(json.get("monitorHeight").is_some());
    assert!(json.get("monitorPositionX").is_some());
    assert!(json.get("monitorPositionY").is_some());
    let back: Monitor = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_canvas_flags() -> CanvasFlags {
    CanvasFlags {
        main: false,
        activate: false,
        mix_audio: false,
        scene_ref: false,
        ephemeral: false,
    }
}

#[test]
fn roundtrip_canvas_flags() {
    let value = sample_canvas_flags();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("MAIN").is_some());
    assert!(json.get("ACTIVATE").is_some());
    assert!(json.get("MIX_AUDIO").is_some());
    assert!(json.get("SCENE_REF").is_some());
    assert!(json.get("EPHEMERAL").is_some());
    let back: CanvasFlags = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_canvas_video_settings() -> CanvasVideoSettings {
    CanvasVideoSettings {
        fps_numerator: None,
        fps_denominator: None,
        base_width: None,
        base_height: None,
        output_width: None,
        output_height: None,
    }
}

#[test]
fn roundtrip_canvas_video_settings() {
    let value = sample_canvas_video_settings();
    let json = serde_json::to_value(&value).unwrap();
    let back: CanvasVideoSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_canvas() -> Canvas {
    Canvas {
        canvas_name: "sample".to_string(),
        canvas_uuid: "sample".to_string(),
        canvas_flags: sample_canvas_flags(),
        canvas_video_settings: sample_canvas_video_settings(),
    }
}

#[test]
fn roundtrip_canvas() {
    let value = sample_canvas();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("canvasName").is_some());
    assert!(json.get("canvasUuid").is_some());
    assert!(json.get("canvasFlags").is_some());
    assert!(json.get("canvasVideoSettings").is_some());
    let back: Canvas = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_key_modifiers() -> KeyModifiers {
    KeyModifiers {
        shift: None,
        control: None,
        alt: None,
        command: None,
    }
}

#[test]
fn roundtrip_key_modifiers() {
    let value = sample_key_modifiers();
    let json = serde_json::to_value(&value).unwrap();
    let back: KeyModifiers = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_property_item() -> PropertyItem {
    PropertyItem {
        item_name: "sample".to_string(),
        item_enabled: false,
        item_value: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_property_item() {
    let value = sample_property_item();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("itemName").is_some());
    assert!(json.get("itemEnabled").is_some());
    assert!(json.get("itemValue").is_some());
    let back: PropertyItem = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_input_volume_meter() -> InputVolumeMeter {
    InputVolumeMeter {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_levels_mul: Vec::new(),
    }
}

#[test]
fn roundtrip_input_volume_meter() {
    let value = sample_input_volume_meter();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputLevelsMul").is_some());
    let back: InputVolumeMeter = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_canvas_list() -> GetCanvasList {
    GetCanvasList {}
}

#[test]
fn roundtrip_get_canvas_list() {
    let value = sample_get_canvas_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetCanvasList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_canvas_list_response() -> GetCanvasListResponse {
    GetCanvasListResponse {
        canvases: Vec::new(),
    }
}

#[test]
fn roundtrip_get_canvas_list_response() {
    let value = sample_get_canvas_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("canvases").is_some());
    let back: GetCanvasListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_persistent_data() -> GetPersistentData {
    GetPersistentData {
        realm: "sample".to_string(),
        slot_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_persistent_data() {
    let value = sample_get_persistent_data();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("realm").is_some());
    assert!(json.get("slotName").is_some());
    let back: GetPersistentData = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_persistent_data_response() -> GetPersistentDataResponse {
    GetPersistentDataResponse {
        slot_value: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_get_persistent_data_response() {
    let value = sample_get_persistent_data_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("slotValue").is_some());
    let back: GetPersistentDataResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_persistent_data() -> SetPersistentData {
    SetPersistentData {
        realm: "sample".to_string(),
        slot_name: "sample".to_string(),
        slot_value: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_set_persistent_data() {
    let value = sample_set_persistent_data();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("realm").is_some());
    assert!(json.get("slotName").is_some());
    assert!(json.get("slotValue").is_some());
    let back: SetPersistentData = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_persistent_data_response() -> SetPersistentDataResponse {
    SetPersistentDataResponse {}
}

#[test]
fn roundtrip_set_persistent_data_response() {
    let value = sample_set_persistent_data_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetPersistentDataResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_collection_list() -> GetSceneCollectionList {
    GetSceneCollectionList {}
}

#[test]
fn roundtrip_get_scene_collection_list() {
    let value = sample_get_scene_collection_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSceneCollectionList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_collection_list_response() -> GetSceneCollectionListResponse {
    GetSceneCollectionListResponse {
        current_scene_collection_name: "sample".to_string(),
        scene_collections: Vec::new(),
    }
}

#[test]
fn roundtrip_get_scene_collection_list_response() {
    let value = sample_get_scene_collection_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("currentSceneCollectionName").is_some());
    assert!(json.get("sceneCollections").is_some());
    let back: GetSceneCollectionListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_scene_collection() -> SetCurrentSceneCollection {
    SetCurrentSceneCollection {
        scene_collection_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_current_scene_collection() {
    let value = sample_set_current_scene_collection();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneCollectionName").is_some());
    let back: SetCurrentSceneCollection = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_scene_collection_response() -> SetCurrentSceneCollectionResponse {
    SetCurrentSceneCollectionResponse {}
}

#[test]
fn roundtrip_set_current_scene_collection_response() {
    let value = sample_set_current_scene_collection_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentSceneCollectionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_scene_collection() -> CreateSceneCollection {
    CreateSceneCollection {
        scene_collection_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_create_scene_collection() {
    let value = sample_create_scene_collection();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneCollectionName").is_some());
    let back: CreateSceneCollection = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_scene_collection_response() -> CreateSceneCollectionResponse {
    CreateSceneCollectionResponse {}
}

#[test]
fn roundtrip_create_scene_collection_response() {
    let value = sample_create_scene_collection_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: CreateSceneCollectionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_profile_list() -> GetProfileList {
    GetProfileList {}
}

#[test]
fn roundtrip_get_profile_list() {
    let value = sample_get_profile_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetProfileList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_profile_list_response() -> GetProfileListResponse {
    GetProfileListResponse {
        current_profile_name: "sample".to_string(),
        profiles: Vec::new(),
    }
}

#[test]
fn roundtrip_get_profile_list_response() {
    let value = sample_get_profile_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("currentProfileName").is_some());
    assert!(json.get("profiles").is_some());
    let back: GetProfileListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_profile() -> SetCurrentProfile {
    SetCurrentProfile {
        profile_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_current_profile() {
    let value = sample_set_current_profile();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("profileName").is_some());
    let back: SetCurrentProfile = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_profile_response() -> SetCurrentProfileResponse {
    SetCurrentProfileResponse {}
}

#[test]
fn roundtrip_set_current_profile_response() {
    let value = sample_set_current_profile_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentProfileResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_profile() -> CreateProfile {
    CreateProfile {
        profile_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_create_profile() {
    let value = sample_create_profile();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("profileName").is_some());
    let back: CreateProfile = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_profile_response() -> CreateProfileResponse {
    CreateProfileResponse {}
}

#[test]
fn roundtrip_create_profile_response() {
    let value = sample_create_profile_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: CreateProfileResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_profile() -> RemoveProfile {
    RemoveProfile {
        profile_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_remove_profile() {
    let value = sample_remove_profile();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("profileName").is_some());
    let back: RemoveProfile = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_profile_response() -> RemoveProfileResponse {
    RemoveProfileResponse {}
}

#[test]
fn roundtrip_remove_profile_response() {
    let value = sample_remove_profile_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: RemoveProfileResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_profile_parameter() -> GetProfileParameter {
    GetProfileParameter {
        parameter_category: "sample".to_string(),
        parameter_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_profile_parameter() {
    let value = sample_get_profile_parameter();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("parameterCategory").is_some());
    assert!(json.get("parameterName").is_some());
    let back: GetProfileParameter = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_profile_parameter_response() -> GetProfileParameterResponse {
    GetProfileParameterResponse {
        parameter_value: None,
        default_parameter_value: None,
    }
}

#[test]
fn roundtrip_get_profile_parameter_response() {
    let value = sample_get_profile_parameter_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetProfileParameterResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_profile_parameter() -> SetProfileParameter {
    SetProfileParameter {
        parameter_category: "sample".to_string(),
        parameter_name: "sample".to_string(),
        parameter_value: None,
    }
}

#[test]
fn roundtrip_set_profile_parameter() {
    let value = sample_set_profile_parameter();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("parameterCategory").is_some());
    assert!(json.get("parameterName").is_some());
    let back: SetProfileParameter = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_profile_parameter_response() -> SetProfileParameterResponse {
    SetProfileParameterResponse {}
}

#[test]
fn roundtrip_set_profile_parameter_response() {
    let value = sample_set_profile_parameter_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetProfileParameterResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_video_settings() -> GetVideoSettings {
    GetVideoSettings {}
}

#[test]
fn roundtrip_get_video_settings() {
    let value = sample_get_video_settings();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetVideoSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_video_settings_response() -> GetVideoSettingsResponse {
    GetVideoSettingsResponse {
        fps_numerator: 0,
        fps_denominator: 0,
        base_width: 0,
        base_height: 0,
        output_width: 0,
        output_height: 0,
    }
}

#[test]
fn roundtrip_get_video_settings_response() {
    let value = sample_get_video_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("fpsNumerator").is_some());
    assert!(json.get("fpsDenominator").is_some());
    assert!(json.get("baseWidth").is_some());
    assert!(json.get("baseHeight").is_some());
    assert!(json.get("outputWidth").is_some());
    assert!(json.get("outputHeight").is_some());
    let back: GetVideoSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_video_settings() -> SetVideoSettings {
    SetVideoSettings {
        fps_numerator: None,
        fps_denominator: None,
        base_width: None,
        base_height: None,
        output_width: None,
        output_height: None,
    }
}

#[test]
fn roundtrip_set_video_settings() {
    let value = sample_set_video_settings();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetVideoSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_video_settings_response() -> SetVideoSettingsResponse {
    SetVideoSettingsResponse {}
}

#[test]
fn roundtrip_set_video_settings_response() {
    let value = sample_set_video_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetVideoSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_stream_service_settings() -> GetStreamServiceSettings {
    GetStreamServiceSettings {}
}

#[test]
fn roundtrip_get_stream_service_settings() {
    let value = sample_get_stream_service_settings();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetStreamServiceSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_stream_service_settings_response() -> GetStreamServiceSettingsResponse {
    GetStreamServiceSettingsResponse {
        stream_service_type: "sample".to_string(),
        stream_service_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_get_stream_service_settings_response() {
    let value = sample_get_stream_service_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("streamServiceType").is_some());
    assert!(json.get("streamServiceSettings").is_some());
    let back: GetStreamServiceSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_stream_service_settings() -> SetStreamServiceSettings {
    SetStreamServiceSettings {
        stream_service_type: "sample".to_string(),
        stream_service_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_set_stream_service_settings() {
    let value = sample_set_stream_service_settings();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("streamServiceType").is_some());
    assert!(json.get("streamServiceSettings").is_some());
    let back: SetStreamServiceSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_stream_service_settings_response() -> SetStreamServiceSettingsResponse {
    SetStreamServiceSettingsResponse {}
}

#[test]
fn roundtrip_set_stream_service_settings_response() {
    let value = sample_set_stream_service_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetStreamServiceSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_record_directory() -> GetRecordDirectory {
    GetRecordDirectory {}
}

#[test]
fn roundtrip_get_record_directory() {
    let value = sample_get_record_directory();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetRecordDirectory = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_record_directory_response() -> GetRecordDirectoryResponse {
    GetRecordDirectoryResponse {
        record_directory: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_record_directory_response() {
    let value = sample_get_record_directory_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("recordDirectory").is_some());
    let back: GetRecordDirectoryResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_record_directory() -> SetRecordDirectory {
    SetRecordDirectory {
        record_directory: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_record_directory() {
    let value = sample_set_record_directory();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("recordDirectory").is_some());
    let back: SetRecordDirectory = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_record_directory_response() -> SetRecordDirectoryResponse {
    SetRecordDirectoryResponse {}
}

#[test]
fn roundtrip_set_record_directory_response() {
    let value = sample_set_record_directory_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetRecordDirectoryResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_filter_kind_list() -> GetSourceFilterKindList {
    GetSourceFilterKindList {}
}

#[test]
fn roundtrip_get_source_filter_kind_list() {
    let value = sample_get_source_filter_kind_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSourceFilterKindList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_filter_kind_list_response() -> GetSourceFilterKindListResponse {
    GetSourceFilterKindListResponse {
        source_filter_kinds: Vec::new(),
    }
}

#[test]
fn roundtrip_get_source_filter_kind_list_response() {
    let value = sample_get_source_filter_kind_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceFilterKinds").is_some());
    let back: GetSourceFilterKindListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_filter_list() -> GetSourceFilterList {
    GetSourceFilterList {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
    }
}

#[test]
fn roundtrip_get_source_filter_list() {
    let value = sample_get_source_filter_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSourceFilterList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_filter_list_response() -> GetSourceFilterListResponse {
    GetSourceFilterListResponse {
        filters: Vec::new(),
    }
}

#[test]
fn roundtrip_get_source_filter_list_response() {
    let value = sample_get_source_filter_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filters").is_some());
    let back: GetSourceFilterListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_filter_default_settings() -> GetSourceFilterDefaultSettings {
    GetSourceFilterDefaultSettings {
        filter_kind: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_source_filter_default_settings() {
    let value = sample_get_source_filter_default_settings();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterKind").is_some());
    let back: GetSourceFilterDefaultSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_filter_default_settings_response() -> GetSourceFilterDefaultSettingsResponse {
    GetSourceFilterDefaultSettingsResponse {
        default_filter_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_get_source_filter_default_settings_response() {
    let value = sample_get_source_filter_default_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("defaultFilterSettings").is_some());
    let back: GetSourceFilterDefaultSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_source_filter() -> CreateSourceFilter {
    CreateSourceFilter {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        filter_name: "sample".to_string(),
        filter_kind: "sample".to_string(),
        filter_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_create_source_filter() {
    let value = sample_create_source_filter();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterName").is_some());
    assert!(json.get("filterKind").is_some());
    assert!(json.get("filterSettings").is_some());
    let back: CreateSourceFilter = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_source_filter_response() -> CreateSourceFilterResponse {
    CreateSourceFilterResponse {}
}

#[test]
fn roundtrip_create_source_filter_response() {
    let value = sample_create_source_filter_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: CreateSourceFilterResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_source_filter() -> RemoveSourceFilter {
    RemoveSourceFilter {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        filter_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_remove_source_filter() {
    let value = sample_remove_source_filter();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterName").is_some());
    let back: RemoveSourceFilter = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_source_filter_response() -> RemoveSourceFilterResponse {
    RemoveSourceFilterResponse {}
}

#[test]
fn roundtrip_remove_source_filter_response() {
    let value = sample_remove_source_filter_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: RemoveSourceFilterResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_source_filter_name() -> SetSourceFilterName {
    SetSourceFilterName {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        filter_name: "sample".to_string(),
        new_filter_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_source_filter_name() {
    let value = sample_set_source_filter_name();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterName").is_some());
    assert!(json.get("newFilterName").is_some());
    let back: SetSourceFilterName = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_source_filter_name_response() -> SetSourceFilterNameResponse {
    SetSourceFilterNameResponse {}
}

#[test]
fn roundtrip_set_source_filter_name_response() {
    let value = sample_set_source_filter_name_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSourceFilterNameResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_filter() -> GetSourceFilter {
    GetSourceFilter {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        filter_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_source_filter() {
    let value = sample_get_source_filter();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterName").is_some());
    let back: GetSourceFilter = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_filter_response() -> GetSourceFilterResponse {
    GetSourceFilterResponse {
        filter_enabled: false,
        filter_index: 0,
        filter_kind: "sample".to_string(),
        filter_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_get_source_filter_response() {
    let value = sample_get_source_filter_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterEnabled").is_some());
    assert!(json.get("filterIndex").is_some());
    assert!(json.get("filterKind").is_some());
    assert!(json.get("filterSettings").is_some());
    let back: GetSourceFilterResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_source_filter_index() -> SetSourceFilterIndex {
    SetSourceFilterIndex {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        filter_name: "sample".to_string(),
        filter_index: 0,
    }
}

#[test]
fn roundtrip_set_source_filter_index() {
    let value = sample_set_source_filter_index();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterName").is_some());
    assert!(json.get("filterIndex").is_some());
    let back: SetSourceFilterIndex = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_source_filter_index_response() -> SetSourceFilterIndexResponse {
    SetSourceFilterIndexResponse {}
}

#[test]
fn roundtrip_set_source_filter_index_response() {
    let value = sample_set_source_filter_index_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSourceFilterIndexResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_source_filter_settings() -> SetSourceFilterSettings {
    SetSourceFilterSettings {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        filter_name: "sample".to_string(),
        filter_settings: serde_json::Value::Null,
        overlay: None,
    }
}

#[test]
fn roundtrip_set_source_filter_settings() {
    let value = sample_set_source_filter_settings();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterName").is_some());
    assert!(json.get("filterSettings").is_some());
    let back: SetSourceFilterSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_source_filter_settings_response() -> SetSourceFilterSettingsResponse {
    SetSourceFilterSettingsResponse {}
}

#[test]
fn roundtrip_set_source_filter_settings_response() {
    let value = sample_set_source_filter_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSourceFilterSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_source_filter_enabled() -> SetSourceFilterEnabled {
    SetSourceFilterEnabled {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        filter_name: "sample".to_string(),
        filter_enabled: false,
    }
}

#[test]
fn roundtrip_set_source_filter_enabled() {
    let value = sample_set_source_filter_enabled();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("filterName").is_some());
    assert!(json.get("filterEnabled").is_some());
    let back: SetSourceFilterEnabled = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_source_filter_enabled_response() -> SetSourceFilterEnabledResponse {
    SetSourceFilterEnabledResponse {}
}

#[test]
fn roundtrip_set_source_filter_enabled_response() {
    let value = sample_set_source_filter_enabled_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSourceFilterEnabledResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_version() -> GetVersion {
    GetVersion {}
}

#[test]
fn roundtrip_get_version() {
    let value = sample_get_version();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetVersion = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_version_response() -> GetVersionResponse {
    GetVersionResponse {
        obs_version: "sample".to_string(),
        obs_web_socket_version: "sample".to_string(),
        rpc_version: 0,
        available_requests: Vec::new(),
        supported_image_formats: Vec::new(),
        platform: "sample".to_string(),
        platform_description: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_version_response() {
    let value = sample_get_version_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("obsVersion").is_some());
    assert!(json.get("obsWebSocketVersion").is_some());
    assert!(json.get("rpcVersion").is_some());
    assert!(json.get("availableRequests").is_some());
    assert!(json.get("supportedImageFormats").is_some());
    assert!(json.get("platform").is_some());
    assert!(json.get("platformDescription").is_some());
    let back: GetVersionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_stats() -> GetStats {
    GetStats {}
}

#[test]
fn roundtrip_get_stats() {
    let value = sample_get_stats();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetStats = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_stats_response() -> GetStatsResponse {
    GetStatsResponse {
        cpu_usage: 0.0,
        memory_usage: 0.0,
        available_disk_space: 0.0,
        active_fps: 0.0,
        average_frame_render_time: 0.0,
        render_skipped_frames: 0,
        render_total_frames: 0,
        output_skipped_frames: 0,
        output_total_frames: 0,
        web_socket_session_incoming_messages: 0,
        web_socket_session_outgoing_messages: 0,
    }
}

#[test]
fn roundtrip_get_stats_response() {
    let value = sample_get_stats_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("cpuUsage").is_some());
    assert!(json.get("memoryUsage").is_some());
    assert!(json.get("availableDiskSpace").is_some());
    assert!(json.get("activeFps").is_some());
    assert!(json.get("averageFrameRenderTime").is_some());
    assert!(json.get("renderSkippedFrames").is_some());
    assert!(json.get("renderTotalFrames").is_some());
    assert!(json.get("outputSkippedFrames").is_some());
    assert!(json.get("outputTotalFrames").is_some());
    assert!(json.get("webSocketSessionIncomingMessages").is_some());
    assert!(json.get("webSocketSessionOutgoingMessages").is_some());
    let back: GetStatsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_broadcast_custom_event() -> BroadcastCustomEvent {
    BroadcastCustomEvent {
        event_data: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_broadcast_custom_event() {
    let value = sample_broadcast_custom_event();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("eventData").is_some());
    let back: BroadcastCustomEvent = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_broadcast_custom_event_response() -> BroadcastCustomEventResponse {
    BroadcastCustomEventResponse {}
}

#[test]
fn roundtrip_broadcast_custom_event_response() {
    let value = sample_broadcast_custom_event_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: BroadcastCustomEventResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_call_vendor_request() -> CallVendorRequest {
    CallVendorRequest {
        vendor_name: "sample".to_string(),
        request_type: "sample".to_string(),
        request_data: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_call_vendor_request() {
    let value = sample_call_vendor_request();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("vendorName").is_some());
    assert!(json.get("requestType").is_some());
    assert!(json.get("requestData").is_some());
    let back: CallVendorRequest = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_call_vendor_request_response() -> CallVendorRequestResponse {
    CallVendorRequestResponse {
        vendor_name: "sample".to_string(),
        request_type: "sample".to_string(),
        response_data: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_call_vendor_request_response() {
    let value = sample_call_vendor_request_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("vendorName").is_some());
    assert!(json.get("requestType").is_some());
    assert!(json.get("responseData").is_some());
    let back: CallVendorRequestResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_hotkey_list() -> GetHotkeyList {
    GetHotkeyList {}
}

#[test]
fn roundtrip_get_hotkey_list() {
    let value = sample_get_hotkey_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetHotkeyList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_hotkey_list_response() -> GetHotkeyListResponse {
    GetHotkeyListResponse {
        hotkeys: Vec::new(),
    }
}

#[test]
fn roundtrip_get_hotkey_list_response() {
    let value = sample_get_hotkey_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("hotkeys").is_some());
    let back: GetHotkeyListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_trigger_hotkey_by_name() -> TriggerHotkeyByName {
    TriggerHotkeyByName {
        hotkey_name: "sample".to_string(),
        context_name: None,
    }
}

#[test]
fn roundtrip_trigger_hotkey_by_name() {
    let value = sample_trigger_hotkey_by_name();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("hotkeyName").is_some());
    let back: TriggerHotkeyByName = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_trigger_hotkey_by_name_response() -> TriggerHotkeyByNameResponse {
    TriggerHotkeyByNameResponse {}
}

#[test]
fn roundtrip_trigger_hotkey_by_name_response() {
    let value = sample_trigger_hotkey_by_name_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: TriggerHotkeyByNameResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_trigger_hotkey_by_key_sequence() -> TriggerHotkeyByKeySequence {
    TriggerHotkeyByKeySequence {
        key_id: None,
        key_modifiers: sample_key_modifiers(),
    }
}

#[test]
fn roundtrip_trigger_hotkey_by_key_sequence() {
    let value = sample_trigger_hotkey_by_key_sequence();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("keyModifiers").is_some());
    let back: TriggerHotkeyByKeySequence = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_trigger_hotkey_by_key_sequence_response() -> TriggerHotkeyByKeySequenceResponse {
    TriggerHotkeyByKeySequenceResponse {}
}

#[test]
fn roundtrip_trigger_hotkey_by_key_sequence_response() {
    let value = sample_trigger_hotkey_by_key_sequence_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: TriggerHotkeyByKeySequenceResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_sleep() -> Sleep {
    Sleep {
        sleep_millis: None,
        sleep_frames: None,
    }
}

#[test]
fn roundtrip_sleep() {
    let value = sample_sleep();
    let json = serde_json::to_value(&value).unwrap();
    let back: Sleep = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_sleep_response() -> SleepResponse {
    SleepResponse {}
}

#[test]
fn roundtrip_sleep_response() {
    let value = sample_sleep_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SleepResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_list() -> GetInputList {
    GetInputList { input_kind: None }
}

#[test]
fn roundtrip_get_input_list() {
    let value = sample_get_input_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_list_response() -> GetInputListResponse {
    GetInputListResponse { inputs: Vec::new() }
}

#[test]
fn roundtrip_get_input_list_response() {
    let value = sample_get_input_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputs").is_some());
    let back: GetInputListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_kind_list() -> GetInputKindList {
    GetInputKindList { unversioned: None }
}

#[test]
fn roundtrip_get_input_kind_list() {
    let value = sample_get_input_kind_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputKindList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_kind_list_response() -> GetInputKindListResponse {
    GetInputKindListResponse {
        input_kinds: Vec::new(),
    }
}

#[test]
fn roundtrip_get_input_kind_list_response() {
    let value = sample_get_input_kind_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputKinds").is_some());
    let back: GetInputKindListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_special_inputs() -> GetSpecialInputs {
    GetSpecialInputs {}
}

#[test]
fn roundtrip_get_special_inputs() {
    let value = sample_get_special_inputs();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSpecialInputs = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_special_inputs_response() -> GetSpecialInputsResponse {
    GetSpecialInputsResponse {
        desktop1: "sample".to_string(),
        desktop2: "sample".to_string(),
        mic1: "sample".to_string(),
        mic2: "sample".to_string(),
        mic3: "sample".to_string(),
        mic4: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_special_inputs_response() {
    let value = sample_get_special_inputs_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("desktop1").is_some());
    assert!(json.get("desktop2").is_some());
    assert!(json.get("mic1").is_some());
    assert!(json.get("mic2").is_some());
    assert!(json.get("mic3").is_some());
    assert!(json.get("mic4").is_some());
    let back: GetSpecialInputsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_input() -> CreateInput {
    CreateInput {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        input_name: "sample".to_string(),
        input_kind: "sample".to_string(),
        input_settings: serde_json::Value::Null,
        scene_item_enabled: None,
    }
}

#[test]
fn roundtrip_create_input() {
    let value = sample_create_input();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputKind").is_some());
    assert!(json.get("inputSettings").is_some());
    let back: CreateInput = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_input_response() -> CreateInputResponse {
    CreateInputResponse {
        input_uuid: "sample".to_string(),
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_create_input_response() {
    let value = sample_create_input_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("sceneItemId").is_some());
    let back: CreateInputResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_input() -> RemoveInput {
    RemoveInput {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_remove_input() {
    let value = sample_remove_input();
    let json = serde_json::to_value(&value).unwrap();
    let back: RemoveInput = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_input_response() -> RemoveInputResponse {
    RemoveInputResponse {}
}

#[test]
fn roundtrip_remove_input_response() {
    let value = sample_remove_input_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: RemoveInputResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_name() -> SetInputName {
    SetInputName {
        input_name: None,
        input_uuid: None,
        new_input_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_input_name() {
    let value = sample_set_input_name();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("newInputName").is_some());
    let back: SetInputName = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_name_response() -> SetInputNameResponse {
    SetInputNameResponse {}
}

#[test]
fn roundtrip_set_input_name_response() {
    let value = sample_set_input_name_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputNameResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_default_settings() -> GetInputDefaultSettings {
    GetInputDefaultSettings {
        input_kind: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_input_default_settings() {
    let value = sample_get_input_default_settings();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputKind").is_some());
    let back: GetInputDefaultSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_default_settings_response() -> GetInputDefaultSettingsResponse {
    GetInputDefaultSettingsResponse {
        default_input_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_get_input_default_settings_response() {
    let value = sample_get_input_default_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("defaultInputSettings").is_some());
    let back: GetInputDefaultSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_settings() -> GetInputSettings {
    GetInputSettings {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_settings() {
    let value = sample_get_input_settings();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_settings_response() -> GetInputSettingsResponse {
    GetInputSettingsResponse {
        input_settings: serde_json::Value::Null,
        input_kind: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_input_settings_response() {
    let value = sample_get_input_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputSettings").is_some());
    assert!(json.get("inputKind").is_some());
    let back: GetInputSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_settings() -> SetInputSettings {
    SetInputSettings {
        input_name: None,
        input_uuid: None,
        input_settings: serde_json::Value::Null,
        overlay: None,
    }
}

#[test]
fn roundtrip_set_input_settings() {
    let value = sample_set_input_settings();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputSettings").is_some());
    let back: SetInputSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_settings_response() -> SetInputSettingsResponse {
    SetInputSettingsResponse {}
}

#[test]
fn roundtrip_set_input_settings_response() {
    let value = sample_set_input_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_mute() -> GetInputMute {
    GetInputMute {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_mute() {
    let value = sample_get_input_mute();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputMute = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_mute_response() -> GetInputMuteResponse {
    GetInputMuteResponse { input_muted: false }
}

#[test]
fn roundtrip_get_input_mute_response() {
    let value = sample_get_input_mute_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputMuted").is_some());
    let back: GetInputMuteResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_mute() -> SetInputMute {
    SetInputMute {
        input_name: None,
        input_uuid: None,
        input_muted: false,
    }
}

#[test]
fn roundtrip_set_input_mute() {
    let value = sample_set_input_mute();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputMuted").is_some());
    let back: SetInputMute = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_mute_response() -> SetInputMuteResponse {
    SetInputMuteResponse {}
}

#[test]
fn roundtrip_set_input_mute_response() {
    let value = sample_set_input_mute_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputMuteResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_input_mute() -> ToggleInputMute {
    ToggleInputMute {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_toggle_input_mute() {
    let value = sample_toggle_input_mute();
    let json = serde_json::to_value(&value).unwrap();
    let back: ToggleInputMute = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_input_mute_response() -> ToggleInputMuteResponse {
    ToggleInputMuteResponse { input_muted: false }
}

#[test]
fn roundtrip_toggle_input_mute_response() {
    let value = sample_toggle_input_mute_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputMuted").is_some());
    let back: ToggleInputMuteResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_volume() -> GetInputVolume {
    GetInputVolume {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_volume() {
    let value = sample_get_input_volume();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputVolume = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_volume_response() -> GetInputVolumeResponse {
    GetInputVolumeResponse {
        input_volume_mul: 0.0,
        input_volume_db: 0.0,
    }
}

#[test]
fn roundtrip_get_input_volume_response() {
    let value = sample_get_input_volume_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputVolumeMul").is_some());
    assert!(json.get("inputVolumeDb").is_some());
    let back: GetInputVolumeResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_volume() -> SetInputVolume {
    SetInputVolume {
        input_name: None,
        input_uuid: None,
        input_volume_mul: None,
        input_volume_db: None,
    }
}

#[test]
fn roundtrip_set_input_volume() {
    let value = sample_set_input_volume();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputVolume = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_volume_response() -> SetInputVolumeResponse {
    SetInputVolumeResponse {}
}

#[test]
fn roundtrip_set_input_volume_response() {
    let value = sample_set_input_volume_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputVolumeResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_audio_balance() -> GetInputAudioBalance {
    GetInputAudioBalance {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_audio_balance() {
    let value = sample_get_input_audio_balance();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputAudioBalance = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_audio_balance_response() -> GetInputAudioBalanceResponse {
    GetInputAudioBalanceResponse {
        input_audio_balance: 0.0,
    }
}

#[test]
fn roundtrip_get_input_audio_balance_response() {
    let value = sample_get_input_audio_balance_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputAudioBalance").is_some());
    let back: GetInputAudioBalanceResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_audio_balance() -> SetInputAudioBalance {
    SetInputAudioBalance {
        input_name: None,
        input_uuid: None,
        input_audio_balance: 0.0,
    }
}

#[test]
fn roundtrip_set_input_audio_balance() {
    let value = sample_set_input_audio_balance();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputAudioBalance").is_some());
    let back: SetInputAudioBalance = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_audio_balance_response() -> SetInputAudioBalanceResponse {
    SetInputAudioBalanceResponse {}
}

#[test]
fn roundtrip_set_input_audio_balance_response() {
    let value = sample_set_input_audio_balance_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputAudioBalanceResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_audio_sync_offset() -> GetInputAudioSyncOffset {
    GetInputAudioSyncOffset {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_audio_sync_offset() {
    let value = sample_get_input_audio_sync_offset();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputAudioSyncOffset = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_audio_sync_offset_response() -> GetInputAudioSyncOffsetResponse {
    GetInputAudioSyncOffsetResponse {
        input_audio_sync_offset: 0,
    }
}

#[test]
fn roundtrip_get_input_audio_sync_offset_response() {
    let value = sample_get_input_audio_sync_offset_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputAudioSyncOffset").is_some());
    let back: GetInputAudioSyncOffsetResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_audio_sync_offset() -> SetInputAudioSyncOffset {
    SetInputAudioSyncOffset {
        input_name: None,
        input_uuid: None,
        input_audio_sync_offset: 0,
    }
}

#[test]
fn roundtrip_set_input_audio_sync_offset() {
    let value = sample_set_input_audio_sync_offset();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputAudioSyncOffset").is_some());
    let back: SetInputAudioSyncOffset = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_audio_sync_offset_response() -> SetInputAudioSyncOffsetResponse {
    SetInputAudioSyncOffsetResponse {}
}

#[test]
fn roundtrip_set_input_audio_sync_offset_response() {
    let value = sample_set_input_audio_sync_offset_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputAudioSyncOffsetResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_audio_monitor_type() -> GetInputAudioMonitorType {
    GetInputAudioMonitorType {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_audio_monitor_type() {
    let value = sample_get_input_audio_monitor_type();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputAudioMonitorType = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_audio_monitor_type_response() -> GetInputAudioMonitorTypeResponse {
    GetInputAudioMonitorTypeResponse {
        monitor_type: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_input_audio_monitor_type_response() {
    let value = sample_get_input_audio_monitor_type_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("monitorType").is_some());
    let back: GetInputAudioMonitorTypeResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_audio_monitor_type() -> SetInputAudioMonitorType {
    SetInputAudioMonitorType {
        input_name: None,
        input_uuid: None,
        monitor_type: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_input_audio_monitor_type() {
    let value = sample_set_input_audio_monitor_type();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("monitorType").is_some());
    let back: SetInputAudioMonitorType = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_audio_monitor_type_response() -> SetInputAudioMonitorTypeResponse {
    SetInputAudioMonitorTypeResponse {}
}

#[test]
fn roundtrip_set_input_audio_monitor_type_response() {
    let value = sample_set_input_audio_monitor_type_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputAudioMonitorTypeResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_audio_tracks() -> GetInputAudioTracks {
    GetInputAudioTracks {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_audio_tracks() {
    let value = sample_get_input_audio_tracks();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputAudioTracks = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_audio_tracks_response() -> GetInputAudioTracksResponse {
    GetInputAudioTracksResponse {
        input_audio_tracks: <AudioTracks as core::default::Default>::default(),
    }
}

#[test]
fn roundtrip_get_input_audio_tracks_response() {
    let value = sample_get_input_audio_tracks_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputAudioTracks").is_some());
    let back: GetInputAudioTracksResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_audio_tracks() -> SetInputAudioTracks {
    SetInputAudioTracks {
        input_name: None,
        input_uuid: None,
        input_audio_tracks: <AudioTracks as core::default::Default>::default(),
    }
}

#[test]
fn roundtrip_set_input_audio_tracks() {
    let value = sample_set_input_audio_tracks();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputAudioTracks").is_some());
    let back: SetInputAudioTracks = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_audio_tracks_response() -> SetInputAudioTracksResponse {
    SetInputAudioTracksResponse {}
}

#[test]
fn roundtrip_set_input_audio_tracks_response() {
    let value = sample_set_input_audio_tracks_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputAudioTracksResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_deinterlace_mode() -> GetInputDeinterlaceMode {
    GetInputDeinterlaceMode {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_deinterlace_mode() {
    let value = sample_get_input_deinterlace_mode();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputDeinterlaceMode = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_deinterlace_mode_response() -> GetInputDeinterlaceModeResponse {
    GetInputDeinterlaceModeResponse {
        input_deinterlace_mode: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_input_deinterlace_mode_response() {
    let value = sample_get_input_deinterlace_mode_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputDeinterlaceMode").is_some());
    let back: GetInputDeinterlaceModeResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_deinterlace_mode() -> SetInputDeinterlaceMode {
    SetInputDeinterlaceMode {
        input_name: None,
        input_uuid: None,
        input_deinterlace_mode: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_input_deinterlace_mode() {
    let value = sample_set_input_deinterlace_mode();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputDeinterlaceMode").is_some());
    let back: SetInputDeinterlaceMode = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_deinterlace_mode_response() -> SetInputDeinterlaceModeResponse {
    SetInputDeinterlaceModeResponse {}
}

#[test]
fn roundtrip_set_input_deinterlace_mode_response() {
    let value = sample_set_input_deinterlace_mode_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputDeinterlaceModeResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_deinterlace_field_order() -> GetInputDeinterlaceFieldOrder {
    GetInputDeinterlaceFieldOrder {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_input_deinterlace_field_order() {
    let value = sample_get_input_deinterlace_field_order();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetInputDeinterlaceFieldOrder = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_deinterlace_field_order_response() -> GetInputDeinterlaceFieldOrderResponse {
    GetInputDeinterlaceFieldOrderResponse {
        input_deinterlace_field_order: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_input_deinterlace_field_order_response() {
    let value = sample_get_input_deinterlace_field_order_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputDeinterlaceFieldOrder").is_some());
    let back: GetInputDeinterlaceFieldOrderResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_deinterlace_field_order() -> SetInputDeinterlaceFieldOrder {
    SetInputDeinterlaceFieldOrder {
        input_name: None,
        input_uuid: None,
        input_deinterlace_field_order: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_input_deinterlace_field_order() {
    let value = sample_set_input_deinterlace_field_order();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputDeinterlaceFieldOrder").is_some());
    let back: SetInputDeinterlaceFieldOrder = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_input_deinterlace_field_order_response() -> SetInputDeinterlaceFieldOrderResponse {
    SetInputDeinterlaceFieldOrderResponse {}
}

#[test]
fn roundtrip_set_input_deinterlace_field_order_response() {
    let value = sample_set_input_deinterlace_field_order_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetInputDeinterlaceFieldOrderResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_properties_list_property_items() -> GetInputPropertiesListPropertyItems {
    GetInputPropertiesListPropertyItems {
        input_name: None,
        input_uuid: None,
        property_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_input_properties_list_property_items() {
    let value = sample_get_input_properties_list_property_items();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("propertyName").is_some());
    let back: GetInputPropertiesListPropertyItems = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_input_properties_list_property_items_response()
-> GetInputPropertiesListPropertyItemsResponse {
    GetInputPropertiesListPropertyItemsResponse {
        property_items: Vec::new(),
    }
}

#[test]
fn roundtrip_get_input_properties_list_property_items_response() {
    let value = sample_get_input_properties_list_property_items_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("propertyItems").is_some());
    let back: GetInputPropertiesListPropertyItemsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_press_input_properties_button() -> PressInputPropertiesButton {
    PressInputPropertiesButton {
        input_name: None,
        input_uuid: None,
        property_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_press_input_properties_button() {
    let value = sample_press_input_properties_button();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("propertyName").is_some());
    let back: PressInputPropertiesButton = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_press_input_properties_button_response() -> PressInputPropertiesButtonResponse {
    PressInputPropertiesButtonResponse {}
}

#[test]
fn roundtrip_press_input_properties_button_response() {
    let value = sample_press_input_properties_button_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: PressInputPropertiesButtonResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_media_input_status() -> GetMediaInputStatus {
    GetMediaInputStatus {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_get_media_input_status() {
    let value = sample_get_media_input_status();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetMediaInputStatus = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_media_input_status_response() -> GetMediaInputStatusResponse {
    GetMediaInputStatusResponse {
        media_state: "sample".to_string(),
        media_duration: None,
        media_cursor: None,
    }
}

#[test]
fn roundtrip_get_media_input_status_response() {
    let value = sample_get_media_input_status_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("mediaState").is_some());
    let back: GetMediaInputStatusResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_media_input_cursor() -> SetMediaInputCursor {
    SetMediaInputCursor {
        input_name: None,
        input_uuid: None,
        media_cursor: 0,
    }
}

#[test]
fn roundtrip_set_media_input_cursor() {
    let value = sample_set_media_input_cursor();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("mediaCursor").is_some());
    let back: SetMediaInputCursor = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_media_input_cursor_response() -> SetMediaInputCursorResponse {
    SetMediaInputCursorResponse {}
}

#[test]
fn roundtrip_set_media_input_cursor_response() {
    let value = sample_set_media_input_cursor_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetMediaInputCursorResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_offset_media_input_cursor() -> OffsetMediaInputCursor {
    OffsetMediaInputCursor {
        input_name: None,
        input_uuid: None,
        media_cursor_offset: 0,
    }
}

#[test]
fn roundtrip_offset_media_input_cursor() {
    let value = sample_offset_media_input_cursor();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("mediaCursorOffset").is_some());
    let back: OffsetMediaInputCursor = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_offset_media_input_cursor_response() -> OffsetMediaInputCursorResponse {
    OffsetMediaInputCursorResponse {}
}

#[test]
fn roundtrip_offset_media_input_cursor_response() {
    let value = sample_offset_media_input_cursor_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: OffsetMediaInputCursorResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_trigger_media_input_action() -> TriggerMediaInputAction {
    TriggerMediaInputAction {
        input_name: None,
        input_uuid: None,
        media_action: "sample".to_string(),
    }
}

#[test]
fn roundtrip_trigger_media_input_action() {
    let value = sample_trigger_media_input_action();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("mediaAction").is_some());
    let back: TriggerMediaInputAction = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_trigger_media_input_action_response() -> TriggerMediaInputActionResponse {
    TriggerMediaInputActionResponse {}
}

#[test]
fn roundtrip_trigger_media_input_action_response() {
    let value = sample_trigger_media_input_action_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: TriggerMediaInputActionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_virtual_cam_status() -> GetVirtualCamStatus {
    GetVirtualCamStatus {}
}

#[test]
fn roundtrip_get_virtual_cam_status() {
    let value = sample_get_virtual_cam_status();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetVirtualCamStatus = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_virtual_cam_status_response() -> GetVirtualCamStatusResponse {
    GetVirtualCamStatusResponse {
        output_active: false,
    }
}

#[test]
fn roundtrip_get_virtual_cam_status_response() {
    let value = sample_get_virtual_cam_status_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    let back: GetVirtualCamStatusResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_virtual_cam() -> ToggleVirtualCam {
    ToggleVirtualCam {}
}

#[test]
fn roundtrip_toggle_virtual_cam() {
    let value = sample_toggle_virtual_cam();
    let json = serde_json::to_value(&value).unwrap();
    let back: ToggleVirtualCam = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_virtual_cam_response() -> ToggleVirtualCamResponse {
    ToggleVirtualCamResponse {
        output_active: false,
    }
}

#[test]
fn roundtrip_toggle_virtual_cam_response() {
    let value = sample_toggle_virtual_cam_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    let back: ToggleVirtualCamResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_virtual_cam() -> StartVirtualCam {
    StartVirtualCam {}
}

#[test]
fn roundtrip_start_virtual_cam() {
    let value = sample_start_virtual_cam();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartVirtualCam = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_virtual_cam_response() -> StartVirtualCamResponse {
    StartVirtualCamResponse {}
}

#[test]
fn roundtrip_start_virtual_cam_response() {
    let value = sample_start_virtual_cam_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartVirtualCamResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_virtual_cam() -> StopVirtualCam {
    StopVirtualCam {}
}

#[test]
fn roundtrip_stop_virtual_cam() {
    let value = sample_stop_virtual_cam();
    let json = serde_json::to_value(&value).unwrap();
    let back: StopVirtualCam = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_virtual_cam_response() -> StopVirtualCamResponse {
    StopVirtualCamResponse {}
}

#[test]
fn roundtrip_stop_virtual_cam_response() {
    let value = sample_stop_virtual_cam_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StopVirtualCamResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_replay_buffer_status() -> GetReplayBufferStatus {
    GetReplayBufferStatus {}
}

#[test]
fn roundtrip_get_replay_buffer_status() {
    let value = sample_get_replay_buffer_status();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetReplayBufferStatus = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_replay_buffer_status_response() -> GetReplayBufferStatusResponse {
    GetReplayBufferStatusResponse {
        output_active: false,
    }
}

#[test]
fn roundtrip_get_replay_buffer_status_response() {
    let value = sample_get_replay_buffer_status_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    let back: GetReplayBufferStatusResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_replay_buffer() -> ToggleReplayBuffer {
    ToggleReplayBuffer {}
}

#[test]
fn roundtrip_toggle_replay_buffer() {
    let value = sample_toggle_replay_buffer();
    let json = serde_json::to_value(&value).unwrap();
    let back: ToggleReplayBuffer = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_replay_buffer_response() -> ToggleReplayBufferResponse {
    ToggleReplayBufferResponse {
        output_active: false,
    }
}

#[test]
fn roundtrip_toggle_replay_buffer_response() {
    let value = sample_toggle_replay_buffer_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    let back: ToggleReplayBufferResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_replay_buffer() -> StartReplayBuffer {
    StartReplayBuffer {}
}

#[test]
fn roundtrip_start_replay_buffer() {
    let value = sample_start_replay_buffer();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartReplayBuffer = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_replay_buffer_response() -> StartReplayBufferResponse {
    StartReplayBufferResponse {}
}

#[test]
fn roundtrip_start_replay_buffer_response() {
    let value = sample_start_replay_buffer_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartReplayBufferResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_replay_buffer() -> StopReplayBuffer {
    StopReplayBuffer {}
}

#[test]
fn roundtrip_stop_replay_buffer() {
    let value = sample_stop_replay_buffer();
    let json = serde_json::to_value(&value).unwrap();
    let back: StopReplayBuffer = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_replay_buffer_response() -> StopReplayBufferResponse {
    StopReplayBufferResponse {}
}

#[test]
fn roundtrip_stop_replay_buffer_response() {
    let value = sample_stop_replay_buffer_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StopReplayBufferResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_save_replay_buffer() -> SaveReplayBuffer {
    SaveReplayBuffer {}
}

#[test]
fn roundtrip_save_replay_buffer() {
    let value = sample_save_replay_buffer();
    let json = serde_json::to_value(&value).unwrap();
    let back: SaveReplayBuffer = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_save_replay_buffer_response() -> SaveReplayBufferResponse {
    SaveReplayBufferResponse {}
}

#[test]
fn roundtrip_save_replay_buffer_response() {
    let value = sample_save_replay_buffer_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SaveReplayBufferResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_last_replay_buffer_replay() -> GetLastReplayBufferReplay {
    GetLastReplayBufferReplay {}
}

#[test]
fn roundtrip_get_last_replay_buffer_replay() {
    let value = sample_get_last_replay_buffer_replay();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetLastReplayBufferReplay = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_last_replay_buffer_replay_response() -> GetLastReplayBufferReplayResponse {
    GetLastReplayBufferReplayResponse {
        saved_replay_path: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_last_replay_buffer_replay_response() {
    let value = sample_get_last_replay_buffer_replay_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("savedReplayPath").is_some());
    let back: GetLastReplayBufferReplayResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_output_list() -> GetOutputList {
    GetOutputList {}
}

#[test]
fn roundtrip_get_output_list() {
    let value = sample_get_output_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetOutputList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_output_list_response() -> GetOutputListResponse {
    GetOutputListResponse {
        outputs: Vec::new(),
    }
}

#[test]
fn roundtrip_get_output_list_response() {
    let value = sample_get_output_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputs").is_some());
    let back: GetOutputListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_output_status() -> GetOutputStatus {
    GetOutputStatus {
        output_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_output_status() {
    let value = sample_get_output_status();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputName").is_some());
    let back: GetOutputStatus = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_output_status_response() -> GetOutputStatusResponse {
    GetOutputStatusResponse {
        output_active: false,
        output_reconnecting: false,
        output_timecode: "sample".to_string(),
        output_duration: 0,
        output_congestion: 0.0,
        output_bytes: 0,
        output_skipped_frames: 0,
        output_total_frames: 0,
    }
}

#[test]
fn roundtrip_get_output_status_response() {
    let value = sample_get_output_status_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    assert!(json.get("outputReconnecting").is_some());
    assert!(json.get("outputTimecode").is_some());
    assert!(json.get("outputDuration").is_some());
    assert!(json.get("outputCongestion").is_some());
    assert!(json.get("outputBytes").is_some());
    assert!(json.get("outputSkippedFrames").is_some());
    assert!(json.get("outputTotalFrames").is_some());
    let back: GetOutputStatusResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_output() -> ToggleOutput {
    ToggleOutput {
        output_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_toggle_output() {
    let value = sample_toggle_output();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputName").is_some());
    let back: ToggleOutput = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_output_response() -> ToggleOutputResponse {
    ToggleOutputResponse {
        output_active: false,
    }
}

#[test]
fn roundtrip_toggle_output_response() {
    let value = sample_toggle_output_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    let back: ToggleOutputResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_output() -> StartOutput {
    StartOutput {
        output_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_start_output() {
    let value = sample_start_output();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputName").is_some());
    let back: StartOutput = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_output_response() -> StartOutputResponse {
    StartOutputResponse {}
}

#[test]
fn roundtrip_start_output_response() {
    let value = sample_start_output_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartOutputResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_output() -> StopOutput {
    StopOutput {
        output_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_stop_output() {
    let value = sample_stop_output();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputName").is_some());
    let back: StopOutput = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_output_response() -> StopOutputResponse {
    StopOutputResponse {}
}

#[test]
fn roundtrip_stop_output_response() {
    let value = sample_stop_output_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StopOutputResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_output_settings() -> GetOutputSettings {
    GetOutputSettings {
        output_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_output_settings() {
    let value = sample_get_output_settings();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputName").is_some());
    let back: GetOutputSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_output_settings_response() -> GetOutputSettingsResponse {
    GetOutputSettingsResponse {
        output_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_get_output_settings_response() {
    let value = sample_get_output_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputSettings").is_some());
    let back: GetOutputSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_output_settings() -> SetOutputSettings {
    SetOutputSettings {
        output_name: "sample".to_string(),
        output_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_set_output_settings() {
    let value = sample_set_output_settings();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputName").is_some());
    assert!(json.get("outputSettings").is_some());
    let back: SetOutputSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_output_settings_response() -> SetOutputSettingsResponse {
    SetOutputSettingsResponse {}
}

#[test]
fn roundtrip_set_output_settings_response() {
    let value = sample_set_output_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetOutputSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_record_status() -> GetRecordStatus {
    GetRecordStatus {}
}

#[test]
fn roundtrip_get_record_status() {
    let value = sample_get_record_status();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetRecordStatus = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_record_status_response() -> GetRecordStatusResponse {
    GetRecordStatusResponse {
        output_active: false,
        output_paused: false,
        output_timecode: "sample".to_string(),
        output_duration: 0,
        output_bytes: 0,
    }
}

#[test]
fn roundtrip_get_record_status_response() {
    let value = sample_get_record_status_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    assert!(json.get("outputPaused").is_some());
    assert!(json.get("outputTimecode").is_some());
    assert!(json.get("outputDuration").is_some());
    assert!(json.get("outputBytes").is_some());
    let back: GetRecordStatusResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_record() -> ToggleRecord {
    ToggleRecord {}
}

#[test]
fn roundtrip_toggle_record() {
    let value = sample_toggle_record();
    let json = serde_json::to_value(&value).unwrap();
    let back: ToggleRecord = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_record_response() -> ToggleRecordResponse {
    ToggleRecordResponse {
        output_active: false,
    }
}

#[test]
fn roundtrip_toggle_record_response() {
    let value = sample_toggle_record_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    let back: ToggleRecordResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_record() -> StartRecord {
    StartRecord {}
}

#[test]
fn roundtrip_start_record() {
    let value = sample_start_record();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartRecord = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_record_response() -> StartRecordResponse {
    StartRecordResponse {}
}

#[test]
fn roundtrip_start_record_response() {
    let value = sample_start_record_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartRecordResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_record() -> StopRecord {
    StopRecord {}
}

#[test]
fn roundtrip_stop_record() {
    let value = sample_stop_record();
    let json = serde_json::to_value(&value).unwrap();
    let back: StopRecord = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_record_response() -> StopRecordResponse {
    StopRecordResponse {
        output_path: "sample".to_string(),
    }
}

#[test]
fn roundtrip_stop_record_response() {
    let value = sample_stop_record_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputPath").is_some());
    let back: StopRecordResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_record_pause() -> ToggleRecordPause {
    ToggleRecordPause {}
}

#[test]
fn roundtrip_toggle_record_pause() {
    let value = sample_toggle_record_pause();
    let json = serde_json::to_value(&value).unwrap();
    let back: ToggleRecordPause = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_record_pause_response() -> ToggleRecordPauseResponse {
    ToggleRecordPauseResponse {}
}

#[test]
fn roundtrip_toggle_record_pause_response() {
    let value = sample_toggle_record_pause_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: ToggleRecordPauseResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_pause_record() -> PauseRecord {
    PauseRecord {}
}

#[test]
fn roundtrip_pause_record() {
    let value = sample_pause_record();
    let json = serde_json::to_value(&value).unwrap();
    let back: PauseRecord = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_pause_record_response() -> PauseRecordResponse {
    PauseRecordResponse {}
}

#[test]
fn roundtrip_pause_record_response() {
    let value = sample_pause_record_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: PauseRecordResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_resume_record() -> ResumeRecord {
    ResumeRecord {}
}

#[test]
fn roundtrip_resume_record() {
    let value = sample_resume_record();
    let json = serde_json::to_value(&value).unwrap();
    let back: ResumeRecord = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_resume_record_response() -> ResumeRecordResponse {
    ResumeRecordResponse {}
}

#[test]
fn roundtrip_resume_record_response() {
    let value = sample_resume_record_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: ResumeRecordResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_split_record_file() -> SplitRecordFile {
    SplitRecordFile {}
}

#[test]
fn roundtrip_split_record_file() {
    let value = sample_split_record_file();
    let json = serde_json::to_value(&value).unwrap();
    let back: SplitRecordFile = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_split_record_file_response() -> SplitRecordFileResponse {
    SplitRecordFileResponse {}
}

#[test]
fn roundtrip_split_record_file_response() {
    let value = sample_split_record_file_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SplitRecordFileResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_record_chapter() -> CreateRecordChapter {
    CreateRecordChapter { chapter_name: None }
}

#[test]
fn roundtrip_create_record_chapter() {
    let value = sample_create_record_chapter();
    let json = serde_json::to_value(&value).unwrap();
    let back: CreateRecordChapter = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_record_chapter_response() -> CreateRecordChapterResponse {
    CreateRecordChapterResponse {}
}

#[test]
fn roundtrip_create_record_chapter_response() {
    let value = sample_create_record_chapter_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: CreateRecordChapterResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_list() -> GetSceneItemList {
    GetSceneItemList {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
    }
}

#[test]
fn roundtrip_get_scene_item_list() {
    let value = sample_get_scene_item_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSceneItemList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_list_response() -> GetSceneItemListResponse {
    GetSceneItemListResponse {
        scene_items: Vec::new(),
    }
}

#[test]
fn roundtrip_get_scene_item_list_response() {
    let value = sample_get_scene_item_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItems").is_some());
    let back: GetSceneItemListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_group_scene_item_list() -> GetGroupSceneItemList {
    GetGroupSceneItemList {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
    }
}

#[test]
fn roundtrip_get_group_scene_item_list() {
    let value = sample_get_group_scene_item_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetGroupSceneItemList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_group_scene_item_list_response() -> GetGroupSceneItemListResponse {
    GetGroupSceneItemListResponse {
        scene_items: Vec::new(),
    }
}

#[test]
fn roundtrip_get_group_scene_item_list_response() {
    let value = sample_get_group_scene_item_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItems").is_some());
    let back: GetGroupSceneItemListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_id() -> GetSceneItemId {
    GetSceneItemId {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        source_name: "sample".to_string(),
        search_offset: None,
    }
}

#[test]
fn roundtrip_get_scene_item_id() {
    let value = sample_get_scene_item_id();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceName").is_some());
    let back: GetSceneItemId = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_id_response() -> GetSceneItemIdResponse {
    GetSceneItemIdResponse { scene_item_id: 0 }
}

#[test]
fn roundtrip_get_scene_item_id_response() {
    let value = sample_get_scene_item_id_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: GetSceneItemIdResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_source() -> GetSceneItemSource {
    GetSceneItemSource {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_get_scene_item_source() {
    let value = sample_get_scene_item_source();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: GetSceneItemSource = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_source_response() -> GetSceneItemSourceResponse {
    GetSceneItemSourceResponse {
        source_name: "sample".to_string(),
        source_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_scene_item_source_response() {
    let value = sample_get_scene_item_source_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceName").is_some());
    assert!(json.get("sourceUuid").is_some());
    let back: GetSceneItemSourceResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_scene_item() -> CreateSceneItem {
    CreateSceneItem {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        source_name: None,
        source_uuid: None,
        scene_item_enabled: None,
    }
}

#[test]
fn roundtrip_create_scene_item() {
    let value = sample_create_scene_item();
    let json = serde_json::to_value(&value).unwrap();
    let back: CreateSceneItem = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_scene_item_response() -> CreateSceneItemResponse {
    CreateSceneItemResponse { scene_item_id: 0 }
}

#[test]
fn roundtrip_create_scene_item_response() {
    let value = sample_create_scene_item_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: CreateSceneItemResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_scene_item() -> RemoveSceneItem {
    RemoveSceneItem {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_remove_scene_item() {
    let value = sample_remove_scene_item();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: RemoveSceneItem = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_scene_item_response() -> RemoveSceneItemResponse {
    RemoveSceneItemResponse {}
}

#[test]
fn roundtrip_remove_scene_item_response() {
    let value = sample_remove_scene_item_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: RemoveSceneItemResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_duplicate_scene_item() -> DuplicateSceneItem {
    DuplicateSceneItem {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
        destination_scene_name: None,
        destination_scene_uuid: None,
    }
}

#[test]
fn roundtrip_duplicate_scene_item() {
    let value = sample_duplicate_scene_item();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: DuplicateSceneItem = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_duplicate_scene_item_response() -> DuplicateSceneItemResponse {
    DuplicateSceneItemResponse { scene_item_id: 0 }
}

#[test]
fn roundtrip_duplicate_scene_item_response() {
    let value = sample_duplicate_scene_item_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: DuplicateSceneItemResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_transform() -> GetSceneItemTransform {
    GetSceneItemTransform {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_get_scene_item_transform() {
    let value = sample_get_scene_item_transform();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: GetSceneItemTransform = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_transform_response() -> GetSceneItemTransformResponse {
    GetSceneItemTransformResponse {
        scene_item_transform: sample_scene_item_transform(),
    }
}

#[test]
fn roundtrip_get_scene_item_transform_response() {
    let value = sample_get_scene_item_transform_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemTransform").is_some());
    let back: GetSceneItemTransformResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_transform() -> SetSceneItemTransform {
    SetSceneItemTransform {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
        scene_item_transform: sample_scene_item_transform_partial(),
    }
}

#[test]
fn roundtrip_set_scene_item_transform() {
    let value = sample_set_scene_item_transform();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemTransform").is_some());
    let back: SetSceneItemTransform = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_transform_response() -> SetSceneItemTransformResponse {
    SetSceneItemTransformResponse {}
}

#[test]
fn roundtrip_set_scene_item_transform_response() {
    let value = sample_set_scene_item_transform_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSceneItemTransformResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_enabled() -> GetSceneItemEnabled {
    GetSceneItemEnabled {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_get_scene_item_enabled() {
    let value = sample_get_scene_item_enabled();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: GetSceneItemEnabled = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_enabled_response() -> GetSceneItemEnabledResponse {
    GetSceneItemEnabledResponse {
        scene_item_enabled: false,
    }
}

#[test]
fn roundtrip_get_scene_item_enabled_response() {
    let value = sample_get_scene_item_enabled_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemEnabled").is_some());
    let back: GetSceneItemEnabledResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_enabled() -> SetSceneItemEnabled {
    SetSceneItemEnabled {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
        scene_item_enabled: false,
    }
}

#[test]
fn roundtrip_set_scene_item_enabled() {
    let value = sample_set_scene_item_enabled();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemEnabled").is_some());
    let back: SetSceneItemEnabled = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_enabled_response() -> SetSceneItemEnabledResponse {
    SetSceneItemEnabledResponse {}
}

#[test]
fn roundtrip_set_scene_item_enabled_response() {
    let value = sample_set_scene_item_enabled_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSceneItemEnabledResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_locked() -> GetSceneItemLocked {
    GetSceneItemLocked {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_get_scene_item_locked() {
    let value = sample_get_scene_item_locked();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: GetSceneItemLocked = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_locked_response() -> GetSceneItemLockedResponse {
    GetSceneItemLockedResponse {
        scene_item_locked: false,
    }
}

#[test]
fn roundtrip_get_scene_item_locked_response() {
    let value = sample_get_scene_item_locked_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemLocked").is_some());
    let back: GetSceneItemLockedResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_locked() -> SetSceneItemLocked {
    SetSceneItemLocked {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
        scene_item_locked: false,
    }
}

#[test]
fn roundtrip_set_scene_item_locked() {
    let value = sample_set_scene_item_locked();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemLocked").is_some());
    let back: SetSceneItemLocked = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_locked_response() -> SetSceneItemLockedResponse {
    SetSceneItemLockedResponse {}
}

#[test]
fn roundtrip_set_scene_item_locked_response() {
    let value = sample_set_scene_item_locked_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSceneItemLockedResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_index() -> GetSceneItemIndex {
    GetSceneItemIndex {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_get_scene_item_index() {
    let value = sample_get_scene_item_index();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: GetSceneItemIndex = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_index_response() -> GetSceneItemIndexResponse {
    GetSceneItemIndexResponse {
        scene_item_index: 0,
    }
}

#[test]
fn roundtrip_get_scene_item_index_response() {
    let value = sample_get_scene_item_index_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemIndex").is_some());
    let back: GetSceneItemIndexResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_index() -> SetSceneItemIndex {
    SetSceneItemIndex {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
        scene_item_index: 0,
    }
}

#[test]
fn roundtrip_set_scene_item_index() {
    let value = sample_set_scene_item_index();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemIndex").is_some());
    let back: SetSceneItemIndex = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_index_response() -> SetSceneItemIndexResponse {
    SetSceneItemIndexResponse {}
}

#[test]
fn roundtrip_set_scene_item_index_response() {
    let value = sample_set_scene_item_index_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSceneItemIndexResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_blend_mode() -> GetSceneItemBlendMode {
    GetSceneItemBlendMode {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_get_scene_item_blend_mode() {
    let value = sample_get_scene_item_blend_mode();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    let back: GetSceneItemBlendMode = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_item_blend_mode_response() -> GetSceneItemBlendModeResponse {
    GetSceneItemBlendModeResponse {
        scene_item_blend_mode: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_scene_item_blend_mode_response() {
    let value = sample_get_scene_item_blend_mode_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemBlendMode").is_some());
    let back: GetSceneItemBlendModeResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_blend_mode() -> SetSceneItemBlendMode {
    SetSceneItemBlendMode {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        scene_item_id: 0,
        scene_item_blend_mode: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_scene_item_blend_mode() {
    let value = sample_set_scene_item_blend_mode();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemBlendMode").is_some());
    let back: SetSceneItemBlendMode = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_item_blend_mode_response() -> SetSceneItemBlendModeResponse {
    SetSceneItemBlendModeResponse {}
}

#[test]
fn roundtrip_set_scene_item_blend_mode_response() {
    let value = sample_set_scene_item_blend_mode_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSceneItemBlendModeResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_list() -> GetSceneList {
    GetSceneList { canvas_uuid: None }
}

#[test]
fn roundtrip_get_scene_list() {
    let value = sample_get_scene_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSceneList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_list_response() -> GetSceneListResponse {
    GetSceneListResponse {
        current_program_scene_name: None,
        current_program_scene_uuid: None,
        current_preview_scene_name: None,
        current_preview_scene_uuid: None,
        scenes: Vec::new(),
    }
}

#[test]
fn roundtrip_get_scene_list_response() {
    let value = sample_get_scene_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("scenes").is_some());
    let back: GetSceneListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_group_list() -> GetGroupList {
    GetGroupList {}
}

#[test]
fn roundtrip_get_group_list() {
    let value = sample_get_group_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetGroupList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_group_list_response() -> GetGroupListResponse {
    GetGroupListResponse { groups: Vec::new() }
}

#[test]
fn roundtrip_get_group_list_response() {
    let value = sample_get_group_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("groups").is_some());
    let back: GetGroupListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_current_program_scene() -> GetCurrentProgramScene {
    GetCurrentProgramScene {}
}

#[test]
fn roundtrip_get_current_program_scene() {
    let value = sample_get_current_program_scene();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetCurrentProgramScene = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_current_program_scene_response() -> GetCurrentProgramSceneResponse {
    GetCurrentProgramSceneResponse {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        current_program_scene_name: "sample".to_string(),
        current_program_scene_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_current_program_scene_response() {
    let value = sample_get_current_program_scene_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("currentProgramSceneName").is_some());
    assert!(json.get("currentProgramSceneUuid").is_some());
    let back: GetCurrentProgramSceneResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_program_scene() -> SetCurrentProgramScene {
    SetCurrentProgramScene {
        scene_name: None,
        scene_uuid: None,
    }
}

#[test]
fn roundtrip_set_current_program_scene() {
    let value = sample_set_current_program_scene();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentProgramScene = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_program_scene_response() -> SetCurrentProgramSceneResponse {
    SetCurrentProgramSceneResponse {}
}

#[test]
fn roundtrip_set_current_program_scene_response() {
    let value = sample_set_current_program_scene_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentProgramSceneResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_current_preview_scene() -> GetCurrentPreviewScene {
    GetCurrentPreviewScene {}
}

#[test]
fn roundtrip_get_current_preview_scene() {
    let value = sample_get_current_preview_scene();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetCurrentPreviewScene = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_current_preview_scene_response() -> GetCurrentPreviewSceneResponse {
    GetCurrentPreviewSceneResponse {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        current_preview_scene_name: "sample".to_string(),
        current_preview_scene_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_current_preview_scene_response() {
    let value = sample_get_current_preview_scene_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("currentPreviewSceneName").is_some());
    assert!(json.get("currentPreviewSceneUuid").is_some());
    let back: GetCurrentPreviewSceneResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_preview_scene() -> SetCurrentPreviewScene {
    SetCurrentPreviewScene {
        scene_name: None,
        scene_uuid: None,
    }
}

#[test]
fn roundtrip_set_current_preview_scene() {
    let value = sample_set_current_preview_scene();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentPreviewScene = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_preview_scene_response() -> SetCurrentPreviewSceneResponse {
    SetCurrentPreviewSceneResponse {}
}

#[test]
fn roundtrip_set_current_preview_scene_response() {
    let value = sample_set_current_preview_scene_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentPreviewSceneResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_scene() -> CreateScene {
    CreateScene {
        canvas_uuid: None,
        scene_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_create_scene() {
    let value = sample_create_scene();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    let back: CreateScene = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_create_scene_response() -> CreateSceneResponse {
    CreateSceneResponse {
        scene_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_create_scene_response() {
    let value = sample_create_scene_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneUuid").is_some());
    let back: CreateSceneResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_scene() -> RemoveScene {
    RemoveScene {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
    }
}

#[test]
fn roundtrip_remove_scene() {
    let value = sample_remove_scene();
    let json = serde_json::to_value(&value).unwrap();
    let back: RemoveScene = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_remove_scene_response() -> RemoveSceneResponse {
    RemoveSceneResponse {}
}

#[test]
fn roundtrip_remove_scene_response() {
    let value = sample_remove_scene_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: RemoveSceneResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_name() -> SetSceneName {
    SetSceneName {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        new_scene_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_scene_name() {
    let value = sample_set_scene_name();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("newSceneName").is_some());
    let back: SetSceneName = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_name_response() -> SetSceneNameResponse {
    SetSceneNameResponse {}
}

#[test]
fn roundtrip_set_scene_name_response() {
    let value = sample_set_scene_name_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSceneNameResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_scene_transition_override() -> GetSceneSceneTransitionOverride {
    GetSceneSceneTransitionOverride {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
    }
}

#[test]
fn roundtrip_get_scene_scene_transition_override() {
    let value = sample_get_scene_scene_transition_override();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSceneSceneTransitionOverride = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_scene_transition_override_response() -> GetSceneSceneTransitionOverrideResponse
{
    GetSceneSceneTransitionOverrideResponse {
        transition_name: None,
        transition_duration: None,
    }
}

#[test]
fn roundtrip_get_scene_scene_transition_override_response() {
    let value = sample_get_scene_scene_transition_override_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSceneSceneTransitionOverrideResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_scene_transition_override() -> SetSceneSceneTransitionOverride {
    SetSceneSceneTransitionOverride {
        canvas_uuid: None,
        scene_name: None,
        scene_uuid: None,
        transition_name: None,
        transition_duration: None,
    }
}

#[test]
fn roundtrip_set_scene_scene_transition_override() {
    let value = sample_set_scene_scene_transition_override();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSceneSceneTransitionOverride = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_scene_scene_transition_override_response() -> SetSceneSceneTransitionOverrideResponse
{
    SetSceneSceneTransitionOverrideResponse {}
}

#[test]
fn roundtrip_set_scene_scene_transition_override_response() {
    let value = sample_set_scene_scene_transition_override_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetSceneSceneTransitionOverrideResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_active() -> GetSourceActive {
    GetSourceActive {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
    }
}

#[test]
fn roundtrip_get_source_active() {
    let value = sample_get_source_active();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSourceActive = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_active_response() -> GetSourceActiveResponse {
    GetSourceActiveResponse {
        video_active: false,
        video_showing: false,
    }
}

#[test]
fn roundtrip_get_source_active_response() {
    let value = sample_get_source_active_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("videoActive").is_some());
    assert!(json.get("videoShowing").is_some());
    let back: GetSourceActiveResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_screenshot() -> GetSourceScreenshot {
    GetSourceScreenshot {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        image_format: "sample".to_string(),
        image_width: None,
        image_height: None,
        image_compression_quality: None,
    }
}

#[test]
fn roundtrip_get_source_screenshot() {
    let value = sample_get_source_screenshot();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("imageFormat").is_some());
    let back: GetSourceScreenshot = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_source_screenshot_response() -> GetSourceScreenshotResponse {
    GetSourceScreenshotResponse {
        image_data: "sample".to_string(),
    }
}

#[test]
fn roundtrip_get_source_screenshot_response() {
    let value = sample_get_source_screenshot_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("imageData").is_some());
    let back: GetSourceScreenshotResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_save_source_screenshot() -> SaveSourceScreenshot {
    SaveSourceScreenshot {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        image_format: "sample".to_string(),
        image_file_path: "sample".to_string(),
        image_width: None,
        image_height: None,
        image_compression_quality: None,
    }
}

#[test]
fn roundtrip_save_source_screenshot() {
    let value = sample_save_source_screenshot();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("imageFormat").is_some());
    assert!(json.get("imageFilePath").is_some());
    let back: SaveSourceScreenshot = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_save_source_screenshot_response() -> SaveSourceScreenshotResponse {
    SaveSourceScreenshotResponse {}
}

#[test]
fn roundtrip_save_source_screenshot_response() {
    let value = sample_save_source_screenshot_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SaveSourceScreenshotResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_stream_status() -> GetStreamStatus {
    GetStreamStatus {}
}

#[test]
fn roundtrip_get_stream_status() {
    let value = sample_get_stream_status();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetStreamStatus = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_stream_status_response() -> GetStreamStatusResponse {
    GetStreamStatusResponse {
        output_active: false,
        output_reconnecting: false,
        output_timecode: "sample".to_string(),
        output_duration: 0,
        output_congestion: 0.0,
        output_bytes: 0,
        output_skipped_frames: 0,
        output_total_frames: 0,
    }
}

#[test]
fn roundtrip_get_stream_status_response() {
    let value = sample_get_stream_status_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    assert!(json.get("outputReconnecting").is_some());
    assert!(json.get("outputTimecode").is_some());
    assert!(json.get("outputDuration").is_some());
    assert!(json.get("outputCongestion").is_some());
    assert!(json.get("outputBytes").is_some());
    assert!(json.get("outputSkippedFrames").is_some());
    assert!(json.get("outputTotalFrames").is_some());
    let back: GetStreamStatusResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_stream() -> ToggleStream {
    ToggleStream {}
}

#[test]
fn roundtrip_toggle_stream() {
    let value = sample_toggle_stream();
    let json = serde_json::to_value(&value).unwrap();
    let back: ToggleStream = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_toggle_stream_response() -> ToggleStreamResponse {
    ToggleStreamResponse {
        output_active: false,
    }
}

#[test]
fn roundtrip_toggle_stream_response() {
    let value = sample_toggle_stream_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    let back: ToggleStreamResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_stream() -> StartStream {
    StartStream {}
}

#[test]
fn roundtrip_start_stream() {
    let value = sample_start_stream();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartStream = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_start_stream_response() -> StartStreamResponse {
    StartStreamResponse {}
}

#[test]
fn roundtrip_start_stream_response() {
    let value = sample_start_stream_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StartStreamResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_stream() -> StopStream {
    StopStream {}
}

#[test]
fn roundtrip_stop_stream() {
    let value = sample_stop_stream();
    let json = serde_json::to_value(&value).unwrap();
    let back: StopStream = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_stop_stream_response() -> StopStreamResponse {
    StopStreamResponse {}
}

#[test]
fn roundtrip_stop_stream_response() {
    let value = sample_stop_stream_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: StopStreamResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_send_stream_caption() -> SendStreamCaption {
    SendStreamCaption {
        caption_text: "sample".to_string(),
    }
}

#[test]
fn roundtrip_send_stream_caption() {
    let value = sample_send_stream_caption();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("captionText").is_some());
    let back: SendStreamCaption = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_send_stream_caption_response() -> SendStreamCaptionResponse {
    SendStreamCaptionResponse {}
}

#[test]
fn roundtrip_send_stream_caption_response() {
    let value = sample_send_stream_caption_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SendStreamCaptionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_transition_kind_list() -> GetTransitionKindList {
    GetTransitionKindList {}
}

#[test]
fn roundtrip_get_transition_kind_list() {
    let value = sample_get_transition_kind_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetTransitionKindList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_transition_kind_list_response() -> GetTransitionKindListResponse {
    GetTransitionKindListResponse {
        transition_kinds: Vec::new(),
    }
}

#[test]
fn roundtrip_get_transition_kind_list_response() {
    let value = sample_get_transition_kind_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionKinds").is_some());
    let back: GetTransitionKindListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_transition_list() -> GetSceneTransitionList {
    GetSceneTransitionList {}
}

#[test]
fn roundtrip_get_scene_transition_list() {
    let value = sample_get_scene_transition_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetSceneTransitionList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_scene_transition_list_response() -> GetSceneTransitionListResponse {
    GetSceneTransitionListResponse {
        current_scene_transition_name: "sample".to_string(),
        current_scene_transition_uuid: "sample".to_string(),
        current_scene_transition_kind: "sample".to_string(),
        transitions: Vec::new(),
    }
}

#[test]
fn roundtrip_get_scene_transition_list_response() {
    let value = sample_get_scene_transition_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("currentSceneTransitionName").is_some());
    assert!(json.get("currentSceneTransitionUuid").is_some());
    assert!(json.get("currentSceneTransitionKind").is_some());
    assert!(json.get("transitions").is_some());
    let back: GetSceneTransitionListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_current_scene_transition() -> GetCurrentSceneTransition {
    GetCurrentSceneTransition {}
}

#[test]
fn roundtrip_get_current_scene_transition() {
    let value = sample_get_current_scene_transition();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetCurrentSceneTransition = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_current_scene_transition_response() -> GetCurrentSceneTransitionResponse {
    GetCurrentSceneTransitionResponse {
        transition_name: "sample".to_string(),
        transition_uuid: "sample".to_string(),
        transition_kind: "sample".to_string(),
        transition_fixed: false,
        transition_duration: None,
        transition_configurable: false,
        transition_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_get_current_scene_transition_response() {
    let value = sample_get_current_scene_transition_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionName").is_some());
    assert!(json.get("transitionUuid").is_some());
    assert!(json.get("transitionKind").is_some());
    assert!(json.get("transitionFixed").is_some());
    assert!(json.get("transitionConfigurable").is_some());
    assert!(json.get("transitionSettings").is_some());
    let back: GetCurrentSceneTransitionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_scene_transition() -> SetCurrentSceneTransition {
    SetCurrentSceneTransition {
        transition_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_set_current_scene_transition() {
    let value = sample_set_current_scene_transition();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionName").is_some());
    let back: SetCurrentSceneTransition = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_scene_transition_response() -> SetCurrentSceneTransitionResponse {
    SetCurrentSceneTransitionResponse {}
}

#[test]
fn roundtrip_set_current_scene_transition_response() {
    let value = sample_set_current_scene_transition_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentSceneTransitionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_scene_transition_duration() -> SetCurrentSceneTransitionDuration {
    SetCurrentSceneTransitionDuration {
        transition_duration: 0,
    }
}

#[test]
fn roundtrip_set_current_scene_transition_duration() {
    let value = sample_set_current_scene_transition_duration();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionDuration").is_some());
    let back: SetCurrentSceneTransitionDuration = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_scene_transition_duration_response()
-> SetCurrentSceneTransitionDurationResponse {
    SetCurrentSceneTransitionDurationResponse {}
}

#[test]
fn roundtrip_set_current_scene_transition_duration_response() {
    let value = sample_set_current_scene_transition_duration_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentSceneTransitionDurationResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_scene_transition_settings() -> SetCurrentSceneTransitionSettings {
    SetCurrentSceneTransitionSettings {
        transition_settings: serde_json::Value::Null,
        overlay: None,
    }
}

#[test]
fn roundtrip_set_current_scene_transition_settings() {
    let value = sample_set_current_scene_transition_settings();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionSettings").is_some());
    let back: SetCurrentSceneTransitionSettings = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_current_scene_transition_settings_response()
-> SetCurrentSceneTransitionSettingsResponse {
    SetCurrentSceneTransitionSettingsResponse {}
}

#[test]
fn roundtrip_set_current_scene_transition_settings_response() {
    let value = sample_set_current_scene_transition_settings_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetCurrentSceneTransitionSettingsResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_current_scene_transition_cursor() -> GetCurrentSceneTransitionCursor {
    GetCurrentSceneTransitionCursor {}
}

#[test]
fn roundtrip_get_current_scene_transition_cursor() {
    let value = sample_get_current_scene_transition_cursor();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetCurrentSceneTransitionCursor = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_current_scene_transition_cursor_response() -> GetCurrentSceneTransitionCursorResponse
{
    GetCurrentSceneTransitionCursorResponse {
        transition_cursor: 0.0,
    }
}

#[test]
fn roundtrip_get_current_scene_transition_cursor_response() {
    let value = sample_get_current_scene_transition_cursor_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionCursor").is_some());
    let back: GetCurrentSceneTransitionCursorResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_trigger_studio_mode_transition() -> TriggerStudioModeTransition {
    TriggerStudioModeTransition {}
}

#[test]
fn roundtrip_trigger_studio_mode_transition() {
    let value = sample_trigger_studio_mode_transition();
    let json = serde_json::to_value(&value).unwrap();
    let back: TriggerStudioModeTransition = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_trigger_studio_mode_transition_response() -> TriggerStudioModeTransitionResponse {
    TriggerStudioModeTransitionResponse {}
}

#[test]
fn roundtrip_trigger_studio_mode_transition_response() {
    let value = sample_trigger_studio_mode_transition_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: TriggerStudioModeTransitionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_t_bar_position() -> SetTBarPosition {
    SetTBarPosition {
        position: 0,
        release: None,
    }
}

#[test]
fn roundtrip_set_t_bar_position() {
    let value = sample_set_t_bar_position();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("position").is_some());
    let back: SetTBarPosition = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_t_bar_position_response() -> SetTBarPositionResponse {
    SetTBarPositionResponse {}
}

#[test]
fn roundtrip_set_t_bar_position_response() {
    let value = sample_set_t_bar_position_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetTBarPositionResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_studio_mode_enabled() -> GetStudioModeEnabled {
    GetStudioModeEnabled {}
}

#[test]
fn roundtrip_get_studio_mode_enabled() {
    let value = sample_get_studio_mode_enabled();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetStudioModeEnabled = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_studio_mode_enabled_response() -> GetStudioModeEnabledResponse {
    GetStudioModeEnabledResponse {
        studio_mode_enabled: false,
    }
}

#[test]
fn roundtrip_get_studio_mode_enabled_response() {
    let value = sample_get_studio_mode_enabled_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("studioModeEnabled").is_some());
    let back: GetStudioModeEnabledResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_studio_mode_enabled() -> SetStudioModeEnabled {
    SetStudioModeEnabled {
        studio_mode_enabled: false,
    }
}

#[test]
fn roundtrip_set_studio_mode_enabled() {
    let value = sample_set_studio_mode_enabled();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("studioModeEnabled").is_some());
    let back: SetStudioModeEnabled = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_set_studio_mode_enabled_response() -> SetStudioModeEnabledResponse {
    SetStudioModeEnabledResponse {}
}

#[test]
fn roundtrip_set_studio_mode_enabled_response() {
    let value = sample_set_studio_mode_enabled_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: SetStudioModeEnabledResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_input_properties_dialog() -> OpenInputPropertiesDialog {
    OpenInputPropertiesDialog {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_open_input_properties_dialog() {
    let value = sample_open_input_properties_dialog();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenInputPropertiesDialog = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_input_properties_dialog_response() -> OpenInputPropertiesDialogResponse {
    OpenInputPropertiesDialogResponse {}
}

#[test]
fn roundtrip_open_input_properties_dialog_response() {
    let value = sample_open_input_properties_dialog_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenInputPropertiesDialogResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_input_filters_dialog() -> OpenInputFiltersDialog {
    OpenInputFiltersDialog {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_open_input_filters_dialog() {
    let value = sample_open_input_filters_dialog();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenInputFiltersDialog = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_input_filters_dialog_response() -> OpenInputFiltersDialogResponse {
    OpenInputFiltersDialogResponse {}
}

#[test]
fn roundtrip_open_input_filters_dialog_response() {
    let value = sample_open_input_filters_dialog_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenInputFiltersDialogResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_input_interact_dialog() -> OpenInputInteractDialog {
    OpenInputInteractDialog {
        input_name: None,
        input_uuid: None,
    }
}

#[test]
fn roundtrip_open_input_interact_dialog() {
    let value = sample_open_input_interact_dialog();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenInputInteractDialog = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_input_interact_dialog_response() -> OpenInputInteractDialogResponse {
    OpenInputInteractDialogResponse {}
}

#[test]
fn roundtrip_open_input_interact_dialog_response() {
    let value = sample_open_input_interact_dialog_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenInputInteractDialogResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_monitor_list() -> GetMonitorList {
    GetMonitorList {}
}

#[test]
fn roundtrip_get_monitor_list() {
    let value = sample_get_monitor_list();
    let json = serde_json::to_value(&value).unwrap();
    let back: GetMonitorList = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_get_monitor_list_response() -> GetMonitorListResponse {
    GetMonitorListResponse {
        monitors: Vec::new(),
    }
}

#[test]
fn roundtrip_get_monitor_list_response() {
    let value = sample_get_monitor_list_response();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("monitors").is_some());
    let back: GetMonitorListResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_video_mix_projector() -> OpenVideoMixProjector {
    OpenVideoMixProjector {
        video_mix_type: "sample".to_string(),
        monitor_index: None,
        projector_geometry: None,
    }
}

#[test]
fn roundtrip_open_video_mix_projector() {
    let value = sample_open_video_mix_projector();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("videoMixType").is_some());
    let back: OpenVideoMixProjector = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_video_mix_projector_response() -> OpenVideoMixProjectorResponse {
    OpenVideoMixProjectorResponse {}
}

#[test]
fn roundtrip_open_video_mix_projector_response() {
    let value = sample_open_video_mix_projector_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenVideoMixProjectorResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_source_projector() -> OpenSourceProjector {
    OpenSourceProjector {
        canvas_uuid: None,
        source_name: None,
        source_uuid: None,
        monitor_index: None,
        projector_geometry: None,
    }
}

#[test]
fn roundtrip_open_source_projector() {
    let value = sample_open_source_projector();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenSourceProjector = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_open_source_projector_response() -> OpenSourceProjectorResponse {
    OpenSourceProjectorResponse {}
}

#[test]
fn roundtrip_open_source_projector_response() {
    let value = sample_open_source_projector_response();
    let json = serde_json::to_value(&value).unwrap();
    let back: OpenSourceProjectorResponse = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

fn sample_canvas_created() -> CanvasCreated {
    CanvasCreated {
        canvas_name: "sample".to_string(),
        canvas_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_canvas_created() {
    let value = sample_canvas_created();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("canvasName").is_some());
    assert!(json.get("canvasUuid").is_some());
    let back: CanvasCreated = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_canvas_created() {
    let value = sample_canvas_created();
    let event = Event::from_parts("CanvasCreated", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "CanvasCreated");
}

fn sample_canvas_removed() -> CanvasRemoved {
    CanvasRemoved {
        canvas_name: "sample".to_string(),
        canvas_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_canvas_removed() {
    let value = sample_canvas_removed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("canvasName").is_some());
    assert!(json.get("canvasUuid").is_some());
    let back: CanvasRemoved = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_canvas_removed() {
    let value = sample_canvas_removed();
    let event = Event::from_parts("CanvasRemoved", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "CanvasRemoved");
}

fn sample_canvas_name_changed() -> CanvasNameChanged {
    CanvasNameChanged {
        canvas_uuid: "sample".to_string(),
        old_canvas_name: "sample".to_string(),
        canvas_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_canvas_name_changed() {
    let value = sample_canvas_name_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("canvasUuid").is_some());
    assert!(json.get("oldCanvasName").is_some());
    assert!(json.get("canvasName").is_some());
    let back: CanvasNameChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_canvas_name_changed() {
    let value = sample_canvas_name_changed();
    let event =
        Event::from_parts("CanvasNameChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "CanvasNameChanged");
}

fn sample_current_scene_collection_changing() -> CurrentSceneCollectionChanging {
    CurrentSceneCollectionChanging {
        scene_collection_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_current_scene_collection_changing() {
    let value = sample_current_scene_collection_changing();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneCollectionName").is_some());
    let back: CurrentSceneCollectionChanging = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_current_scene_collection_changing() {
    let value = sample_current_scene_collection_changing();
    let event = Event::from_parts(
        "CurrentSceneCollectionChanging",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "CurrentSceneCollectionChanging");
}

fn sample_current_scene_collection_changed() -> CurrentSceneCollectionChanged {
    CurrentSceneCollectionChanged {
        scene_collection_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_current_scene_collection_changed() {
    let value = sample_current_scene_collection_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneCollectionName").is_some());
    let back: CurrentSceneCollectionChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_current_scene_collection_changed() {
    let value = sample_current_scene_collection_changed();
    let event = Event::from_parts(
        "CurrentSceneCollectionChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "CurrentSceneCollectionChanged");
}

fn sample_scene_collection_list_changed() -> SceneCollectionListChanged {
    SceneCollectionListChanged {
        scene_collections: Vec::new(),
    }
}

#[test]
fn roundtrip_scene_collection_list_changed() {
    let value = sample_scene_collection_list_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneCollections").is_some());
    let back: SceneCollectionListChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_collection_list_changed() {
    let value = sample_scene_collection_list_changed();
    let event = Event::from_parts(
        "SceneCollectionListChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SceneCollectionListChanged");
}

fn sample_current_profile_changing() -> CurrentProfileChanging {
    CurrentProfileChanging {
        profile_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_current_profile_changing() {
    let value = sample_current_profile_changing();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("profileName").is_some());
    let back: CurrentProfileChanging = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_current_profile_changing() {
    let value = sample_current_profile_changing();
    let event = Event::from_parts(
        "CurrentProfileChanging",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "CurrentProfileChanging");
}

fn sample_current_profile_changed() -> CurrentProfileChanged {
    CurrentProfileChanged {
        profile_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_current_profile_changed() {
    let value = sample_current_profile_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("profileName").is_some());
    let back: CurrentProfileChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_current_profile_changed() {
    let value = sample_current_profile_changed();
    let event = Event::from_parts(
        "CurrentProfileChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "CurrentProfileChanged");
}

fn sample_profile_list_changed() -> ProfileListChanged {
    ProfileListChanged {
        profiles: Vec::new(),
    }
}

#[test]
fn roundtrip_profile_list_changed() {
    let value = sample_profile_list_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("profiles").is_some());
    let back: ProfileListChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_profile_list_changed() {
    let value = sample_profile_list_changed();
    let event =
        Event::from_parts("ProfileListChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "ProfileListChanged");
}

fn sample_source_filter_list_reindexed() -> SourceFilterListReindexed {
    SourceFilterListReindexed {
        source_name: "sample".to_string(),
        filters: Vec::new(),
    }
}

#[test]
fn roundtrip_source_filter_list_reindexed() {
    let value = sample_source_filter_list_reindexed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceName").is_some());
    assert!(json.get("filters").is_some());
    let back: SourceFilterListReindexed = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_source_filter_list_reindexed() {
    let value = sample_source_filter_list_reindexed();
    let event = Event::from_parts(
        "SourceFilterListReindexed",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SourceFilterListReindexed");
}

fn sample_source_filter_created() -> SourceFilterCreated {
    SourceFilterCreated {
        source_name: "sample".to_string(),
        filter_name: "sample".to_string(),
        filter_kind: "sample".to_string(),
        filter_index: 0,
        filter_settings: serde_json::Value::Null,
        default_filter_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_source_filter_created() {
    let value = sample_source_filter_created();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceName").is_some());
    assert!(json.get("filterName").is_some());
    assert!(json.get("filterKind").is_some());
    assert!(json.get("filterIndex").is_some());
    assert!(json.get("filterSettings").is_some());
    assert!(json.get("defaultFilterSettings").is_some());
    let back: SourceFilterCreated = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_source_filter_created() {
    let value = sample_source_filter_created();
    let event =
        Event::from_parts("SourceFilterCreated", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SourceFilterCreated");
}

fn sample_source_filter_removed() -> SourceFilterRemoved {
    SourceFilterRemoved {
        source_name: "sample".to_string(),
        filter_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_source_filter_removed() {
    let value = sample_source_filter_removed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceName").is_some());
    assert!(json.get("filterName").is_some());
    let back: SourceFilterRemoved = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_source_filter_removed() {
    let value = sample_source_filter_removed();
    let event =
        Event::from_parts("SourceFilterRemoved", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SourceFilterRemoved");
}

fn sample_source_filter_name_changed() -> SourceFilterNameChanged {
    SourceFilterNameChanged {
        source_name: "sample".to_string(),
        old_filter_name: "sample".to_string(),
        filter_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_source_filter_name_changed() {
    let value = sample_source_filter_name_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceName").is_some());
    assert!(json.get("oldFilterName").is_some());
    assert!(json.get("filterName").is_some());
    let back: SourceFilterNameChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_source_filter_name_changed() {
    let value = sample_source_filter_name_changed();
    let event = Event::from_parts(
        "SourceFilterNameChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SourceFilterNameChanged");
}

fn sample_source_filter_settings_changed() -> SourceFilterSettingsChanged {
    SourceFilterSettingsChanged {
        source_name: "sample".to_string(),
        filter_name: "sample".to_string(),
        filter_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_source_filter_settings_changed() {
    let value = sample_source_filter_settings_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceName").is_some());
    assert!(json.get("filterName").is_some());
    assert!(json.get("filterSettings").is_some());
    let back: SourceFilterSettingsChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_source_filter_settings_changed() {
    let value = sample_source_filter_settings_changed();
    let event = Event::from_parts(
        "SourceFilterSettingsChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SourceFilterSettingsChanged");
}

fn sample_source_filter_enable_state_changed() -> SourceFilterEnableStateChanged {
    SourceFilterEnableStateChanged {
        source_name: "sample".to_string(),
        filter_name: "sample".to_string(),
        filter_enabled: false,
    }
}

#[test]
fn roundtrip_source_filter_enable_state_changed() {
    let value = sample_source_filter_enable_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sourceName").is_some());
    assert!(json.get("filterName").is_some());
    assert!(json.get("filterEnabled").is_some());
    let back: SourceFilterEnableStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_source_filter_enable_state_changed() {
    let value = sample_source_filter_enable_state_changed();
    let event = Event::from_parts(
        "SourceFilterEnableStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SourceFilterEnableStateChanged");
}

fn sample_exit_started() -> ExitStarted {
    ExitStarted {}
}

#[test]
fn roundtrip_exit_started() {
    let value = sample_exit_started();
    let json = serde_json::to_value(&value).unwrap();
    let back: ExitStarted = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_exit_started() {
    let value = sample_exit_started();
    let event = Event::from_parts("ExitStarted", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "ExitStarted");
}

fn sample_input_created() -> InputCreated {
    InputCreated {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_kind: "sample".to_string(),
        unversioned_input_kind: "sample".to_string(),
        input_kind_caps: 0,
        input_settings: serde_json::Value::Null,
        default_input_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_input_created() {
    let value = sample_input_created();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputKind").is_some());
    assert!(json.get("unversionedInputKind").is_some());
    assert!(json.get("inputKindCaps").is_some());
    assert!(json.get("inputSettings").is_some());
    assert!(json.get("defaultInputSettings").is_some());
    let back: InputCreated = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_created() {
    let value = sample_input_created();
    let event = Event::from_parts("InputCreated", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "InputCreated");
}

fn sample_input_removed() -> InputRemoved {
    InputRemoved {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_input_removed() {
    let value = sample_input_removed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    let back: InputRemoved = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_removed() {
    let value = sample_input_removed();
    let event = Event::from_parts("InputRemoved", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "InputRemoved");
}

fn sample_input_name_changed() -> InputNameChanged {
    InputNameChanged {
        input_uuid: "sample".to_string(),
        old_input_name: "sample".to_string(),
        input_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_input_name_changed() {
    let value = sample_input_name_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("oldInputName").is_some());
    assert!(json.get("inputName").is_some());
    let back: InputNameChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_name_changed() {
    let value = sample_input_name_changed();
    let event =
        Event::from_parts("InputNameChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "InputNameChanged");
}

fn sample_input_settings_changed() -> InputSettingsChanged {
    InputSettingsChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_settings: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_input_settings_changed() {
    let value = sample_input_settings_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputSettings").is_some());
    let back: InputSettingsChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_settings_changed() {
    let value = sample_input_settings_changed();
    let event = Event::from_parts(
        "InputSettingsChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "InputSettingsChanged");
}

fn sample_input_active_state_changed() -> InputActiveStateChanged {
    InputActiveStateChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        video_active: false,
    }
}

#[test]
fn roundtrip_input_active_state_changed() {
    let value = sample_input_active_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("videoActive").is_some());
    let back: InputActiveStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_active_state_changed() {
    let value = sample_input_active_state_changed();
    let event = Event::from_parts(
        "InputActiveStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "InputActiveStateChanged");
}

fn sample_input_show_state_changed() -> InputShowStateChanged {
    InputShowStateChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        video_showing: false,
    }
}

#[test]
fn roundtrip_input_show_state_changed() {
    let value = sample_input_show_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("videoShowing").is_some());
    let back: InputShowStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_show_state_changed() {
    let value = sample_input_show_state_changed();
    let event = Event::from_parts(
        "InputShowStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "InputShowStateChanged");
}

fn sample_input_mute_state_changed() -> InputMuteStateChanged {
    InputMuteStateChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_muted: false,
    }
}

#[test]
fn roundtrip_input_mute_state_changed() {
    let value = sample_input_mute_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputMuted").is_some());
    let back: InputMuteStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_mute_state_changed() {
    let value = sample_input_mute_state_changed();
    let event = Event::from_parts(
        "InputMuteStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "InputMuteStateChanged");
}

fn sample_input_volume_changed() -> InputVolumeChanged {
    InputVolumeChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_volume_mul: 0.0,
        input_volume_db: 0.0,
    }
}

#[test]
fn roundtrip_input_volume_changed() {
    let value = sample_input_volume_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputVolumeMul").is_some());
    assert!(json.get("inputVolumeDb").is_some());
    let back: InputVolumeChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_volume_changed() {
    let value = sample_input_volume_changed();
    let event =
        Event::from_parts("InputVolumeChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "InputVolumeChanged");
}

fn sample_input_audio_balance_changed() -> InputAudioBalanceChanged {
    InputAudioBalanceChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_audio_balance: 0.0,
    }
}

#[test]
fn roundtrip_input_audio_balance_changed() {
    let value = sample_input_audio_balance_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputAudioBalance").is_some());
    let back: InputAudioBalanceChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_audio_balance_changed() {
    let value = sample_input_audio_balance_changed();
    let event = Event::from_parts(
        "InputAudioBalanceChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "InputAudioBalanceChanged");
}

fn sample_input_audio_sync_offset_changed() -> InputAudioSyncOffsetChanged {
    InputAudioSyncOffsetChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_audio_sync_offset: 0,
    }
}

#[test]
fn roundtrip_input_audio_sync_offset_changed() {
    let value = sample_input_audio_sync_offset_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputAudioSyncOffset").is_some());
    let back: InputAudioSyncOffsetChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_audio_sync_offset_changed() {
    let value = sample_input_audio_sync_offset_changed();
    let event = Event::from_parts(
        "InputAudioSyncOffsetChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "InputAudioSyncOffsetChanged");
}

fn sample_input_audio_tracks_changed() -> InputAudioTracksChanged {
    InputAudioTracksChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        input_audio_tracks: <AudioTracks as core::default::Default>::default(),
    }
}

#[test]
fn roundtrip_input_audio_tracks_changed() {
    let value = sample_input_audio_tracks_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("inputAudioTracks").is_some());
    let back: InputAudioTracksChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_audio_tracks_changed() {
    let value = sample_input_audio_tracks_changed();
    let event = Event::from_parts(
        "InputAudioTracksChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "InputAudioTracksChanged");
}

fn sample_input_audio_monitor_type_changed() -> InputAudioMonitorTypeChanged {
    InputAudioMonitorTypeChanged {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        monitor_type: "sample".to_string(),
    }
}

#[test]
fn roundtrip_input_audio_monitor_type_changed() {
    let value = sample_input_audio_monitor_type_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("monitorType").is_some());
    let back: InputAudioMonitorTypeChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_audio_monitor_type_changed() {
    let value = sample_input_audio_monitor_type_changed();
    let event = Event::from_parts(
        "InputAudioMonitorTypeChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "InputAudioMonitorTypeChanged");
}

fn sample_input_volume_meters() -> InputVolumeMeters {
    InputVolumeMeters { inputs: Vec::new() }
}

#[test]
fn roundtrip_input_volume_meters() {
    let value = sample_input_volume_meters();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputs").is_some());
    let back: InputVolumeMeters = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_input_volume_meters() {
    let value = sample_input_volume_meters();
    let event =
        Event::from_parts("InputVolumeMeters", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "InputVolumeMeters");
}

fn sample_media_input_playback_started() -> MediaInputPlaybackStarted {
    MediaInputPlaybackStarted {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_media_input_playback_started() {
    let value = sample_media_input_playback_started();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    let back: MediaInputPlaybackStarted = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_media_input_playback_started() {
    let value = sample_media_input_playback_started();
    let event = Event::from_parts(
        "MediaInputPlaybackStarted",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "MediaInputPlaybackStarted");
}

fn sample_media_input_playback_ended() -> MediaInputPlaybackEnded {
    MediaInputPlaybackEnded {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_media_input_playback_ended() {
    let value = sample_media_input_playback_ended();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    let back: MediaInputPlaybackEnded = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_media_input_playback_ended() {
    let value = sample_media_input_playback_ended();
    let event = Event::from_parts(
        "MediaInputPlaybackEnded",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "MediaInputPlaybackEnded");
}

fn sample_media_input_action_triggered() -> MediaInputActionTriggered {
    MediaInputActionTriggered {
        input_name: "sample".to_string(),
        input_uuid: "sample".to_string(),
        media_action: "sample".to_string(),
    }
}

#[test]
fn roundtrip_media_input_action_triggered() {
    let value = sample_media_input_action_triggered();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("inputName").is_some());
    assert!(json.get("inputUuid").is_some());
    assert!(json.get("mediaAction").is_some());
    let back: MediaInputActionTriggered = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_media_input_action_triggered() {
    let value = sample_media_input_action_triggered();
    let event = Event::from_parts(
        "MediaInputActionTriggered",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "MediaInputActionTriggered");
}

fn sample_stream_state_changed() -> StreamStateChanged {
    StreamStateChanged {
        output_active: false,
        output_state: "sample".to_string(),
    }
}

#[test]
fn roundtrip_stream_state_changed() {
    let value = sample_stream_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    assert!(json.get("outputState").is_some());
    let back: StreamStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_stream_state_changed() {
    let value = sample_stream_state_changed();
    let event =
        Event::from_parts("StreamStateChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "StreamStateChanged");
}

fn sample_record_state_changed() -> RecordStateChanged {
    RecordStateChanged {
        output_active: false,
        output_state: "sample".to_string(),
        output_path: None,
    }
}

#[test]
fn roundtrip_record_state_changed() {
    let value = sample_record_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    assert!(json.get("outputState").is_some());
    let back: RecordStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_record_state_changed() {
    let value = sample_record_state_changed();
    let event =
        Event::from_parts("RecordStateChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "RecordStateChanged");
}

fn sample_record_file_changed() -> RecordFileChanged {
    RecordFileChanged {
        new_output_path: "sample".to_string(),
    }
}

#[test]
fn roundtrip_record_file_changed() {
    let value = sample_record_file_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("newOutputPath").is_some());
    let back: RecordFileChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_record_file_changed() {
    let value = sample_record_file_changed();
    let event =
        Event::from_parts("RecordFileChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "RecordFileChanged");
}

fn sample_replay_buffer_state_changed() -> ReplayBufferStateChanged {
    ReplayBufferStateChanged {
        output_active: false,
        output_state: "sample".to_string(),
    }
}

#[test]
fn roundtrip_replay_buffer_state_changed() {
    let value = sample_replay_buffer_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    assert!(json.get("outputState").is_some());
    let back: ReplayBufferStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_replay_buffer_state_changed() {
    let value = sample_replay_buffer_state_changed();
    let event = Event::from_parts(
        "ReplayBufferStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "ReplayBufferStateChanged");
}

fn sample_virtualcam_state_changed() -> VirtualcamStateChanged {
    VirtualcamStateChanged {
        output_active: false,
        output_state: "sample".to_string(),
    }
}

#[test]
fn roundtrip_virtualcam_state_changed() {
    let value = sample_virtualcam_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("outputActive").is_some());
    assert!(json.get("outputState").is_some());
    let back: VirtualcamStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_virtualcam_state_changed() {
    let value = sample_virtualcam_state_changed();
    let event = Event::from_parts(
        "VirtualcamStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "VirtualcamStateChanged");
}

fn sample_replay_buffer_saved() -> ReplayBufferSaved {
    ReplayBufferSaved {
        saved_replay_path: "sample".to_string(),
    }
}

#[test]
fn roundtrip_replay_buffer_saved() {
    let value = sample_replay_buffer_saved();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("savedReplayPath").is_some());
    let back: ReplayBufferSaved = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_replay_buffer_saved() {
    let value = sample_replay_buffer_saved();
    let event =
        Event::from_parts("ReplayBufferSaved", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "ReplayBufferSaved");
}

fn sample_scene_item_created() -> SceneItemCreated {
    SceneItemCreated {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        source_name: "sample".to_string(),
        source_uuid: "sample".to_string(),
        scene_item_id: 0,
        scene_item_index: 0,
    }
}

#[test]
fn roundtrip_scene_item_created() {
    let value = sample_scene_item_created();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("sourceName").is_some());
    assert!(json.get("sourceUuid").is_some());
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemIndex").is_some());
    let back: SceneItemCreated = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_item_created() {
    let value = sample_scene_item_created();
    let event =
        Event::from_parts("SceneItemCreated", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SceneItemCreated");
}

fn sample_scene_item_removed() -> SceneItemRemoved {
    SceneItemRemoved {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        source_name: "sample".to_string(),
        source_uuid: "sample".to_string(),
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_scene_item_removed() {
    let value = sample_scene_item_removed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("sourceName").is_some());
    assert!(json.get("sourceUuid").is_some());
    assert!(json.get("sceneItemId").is_some());
    let back: SceneItemRemoved = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_item_removed() {
    let value = sample_scene_item_removed();
    let event =
        Event::from_parts("SceneItemRemoved", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SceneItemRemoved");
}

fn sample_scene_item_list_reindexed() -> SceneItemListReindexed {
    SceneItemListReindexed {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        scene_items: Vec::new(),
    }
}

#[test]
fn roundtrip_scene_item_list_reindexed() {
    let value = sample_scene_item_list_reindexed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("sceneItems").is_some());
    let back: SceneItemListReindexed = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_item_list_reindexed() {
    let value = sample_scene_item_list_reindexed();
    let event = Event::from_parts(
        "SceneItemListReindexed",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SceneItemListReindexed");
}

fn sample_scene_item_enable_state_changed() -> SceneItemEnableStateChanged {
    SceneItemEnableStateChanged {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        scene_item_id: 0,
        scene_item_enabled: false,
    }
}

#[test]
fn roundtrip_scene_item_enable_state_changed() {
    let value = sample_scene_item_enable_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemEnabled").is_some());
    let back: SceneItemEnableStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_item_enable_state_changed() {
    let value = sample_scene_item_enable_state_changed();
    let event = Event::from_parts(
        "SceneItemEnableStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SceneItemEnableStateChanged");
}

fn sample_scene_item_lock_state_changed() -> SceneItemLockStateChanged {
    SceneItemLockStateChanged {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        scene_item_id: 0,
        scene_item_locked: false,
    }
}

#[test]
fn roundtrip_scene_item_lock_state_changed() {
    let value = sample_scene_item_lock_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemLocked").is_some());
    let back: SceneItemLockStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_item_lock_state_changed() {
    let value = sample_scene_item_lock_state_changed();
    let event = Event::from_parts(
        "SceneItemLockStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SceneItemLockStateChanged");
}

fn sample_scene_item_selected() -> SceneItemSelected {
    SceneItemSelected {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        scene_item_id: 0,
    }
}

#[test]
fn roundtrip_scene_item_selected() {
    let value = sample_scene_item_selected();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("sceneItemId").is_some());
    let back: SceneItemSelected = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_item_selected() {
    let value = sample_scene_item_selected();
    let event =
        Event::from_parts("SceneItemSelected", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SceneItemSelected");
}

fn sample_scene_item_transform_changed() -> SceneItemTransformChanged {
    SceneItemTransformChanged {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        scene_item_id: 0,
        scene_item_transform: sample_scene_item_transform(),
    }
}

#[test]
fn roundtrip_scene_item_transform_changed() {
    let value = sample_scene_item_transform_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("sceneItemId").is_some());
    assert!(json.get("sceneItemTransform").is_some());
    let back: SceneItemTransformChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_item_transform_changed() {
    let value = sample_scene_item_transform_changed();
    let event = Event::from_parts(
        "SceneItemTransformChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SceneItemTransformChanged");
}

fn sample_scene_created() -> SceneCreated {
    SceneCreated {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        is_group: false,
    }
}

#[test]
fn roundtrip_scene_created() {
    let value = sample_scene_created();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("isGroup").is_some());
    let back: SceneCreated = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_created() {
    let value = sample_scene_created();
    let event = Event::from_parts("SceneCreated", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SceneCreated");
}

fn sample_scene_removed() -> SceneRemoved {
    SceneRemoved {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
        is_group: false,
    }
}

#[test]
fn roundtrip_scene_removed() {
    let value = sample_scene_removed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("isGroup").is_some());
    let back: SceneRemoved = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_removed() {
    let value = sample_scene_removed();
    let event = Event::from_parts("SceneRemoved", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SceneRemoved");
}

fn sample_scene_name_changed() -> SceneNameChanged {
    SceneNameChanged {
        scene_uuid: "sample".to_string(),
        old_scene_name: "sample".to_string(),
        scene_name: "sample".to_string(),
    }
}

#[test]
fn roundtrip_scene_name_changed() {
    let value = sample_scene_name_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneUuid").is_some());
    assert!(json.get("oldSceneName").is_some());
    assert!(json.get("sceneName").is_some());
    let back: SceneNameChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_name_changed() {
    let value = sample_scene_name_changed();
    let event =
        Event::from_parts("SceneNameChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SceneNameChanged");
}

fn sample_current_program_scene_changed() -> CurrentProgramSceneChanged {
    CurrentProgramSceneChanged {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_current_program_scene_changed() {
    let value = sample_current_program_scene_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    let back: CurrentProgramSceneChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_current_program_scene_changed() {
    let value = sample_current_program_scene_changed();
    let event = Event::from_parts(
        "CurrentProgramSceneChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "CurrentProgramSceneChanged");
}

fn sample_current_preview_scene_changed() -> CurrentPreviewSceneChanged {
    CurrentPreviewSceneChanged {
        scene_name: "sample".to_string(),
        scene_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_current_preview_scene_changed() {
    let value = sample_current_preview_scene_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("sceneName").is_some());
    assert!(json.get("sceneUuid").is_some());
    let back: CurrentPreviewSceneChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_current_preview_scene_changed() {
    let value = sample_current_preview_scene_changed();
    let event = Event::from_parts(
        "CurrentPreviewSceneChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "CurrentPreviewSceneChanged");
}

fn sample_scene_list_changed() -> SceneListChanged {
    SceneListChanged { scenes: Vec::new() }
}

#[test]
fn roundtrip_scene_list_changed() {
    let value = sample_scene_list_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("scenes").is_some());
    let back: SceneListChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_list_changed() {
    let value = sample_scene_list_changed();
    let event =
        Event::from_parts("SceneListChanged", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "SceneListChanged");
}

fn sample_current_scene_transition_changed() -> CurrentSceneTransitionChanged {
    CurrentSceneTransitionChanged {
        transition_name: "sample".to_string(),
        transition_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_current_scene_transition_changed() {
    let value = sample_current_scene_transition_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionName").is_some());
    assert!(json.get("transitionUuid").is_some());
    let back: CurrentSceneTransitionChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_current_scene_transition_changed() {
    let value = sample_current_scene_transition_changed();
    let event = Event::from_parts(
        "CurrentSceneTransitionChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "CurrentSceneTransitionChanged");
}

fn sample_current_scene_transition_duration_changed() -> CurrentSceneTransitionDurationChanged {
    CurrentSceneTransitionDurationChanged {
        transition_duration: 0,
    }
}

#[test]
fn roundtrip_current_scene_transition_duration_changed() {
    let value = sample_current_scene_transition_duration_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionDuration").is_some());
    let back: CurrentSceneTransitionDurationChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_current_scene_transition_duration_changed() {
    let value = sample_current_scene_transition_duration_changed();
    let event = Event::from_parts(
        "CurrentSceneTransitionDurationChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "CurrentSceneTransitionDurationChanged");
}

fn sample_scene_transition_started() -> SceneTransitionStarted {
    SceneTransitionStarted {
        transition_name: "sample".to_string(),
        transition_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_scene_transition_started() {
    let value = sample_scene_transition_started();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionName").is_some());
    assert!(json.get("transitionUuid").is_some());
    let back: SceneTransitionStarted = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_transition_started() {
    let value = sample_scene_transition_started();
    let event = Event::from_parts(
        "SceneTransitionStarted",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SceneTransitionStarted");
}

fn sample_scene_transition_ended() -> SceneTransitionEnded {
    SceneTransitionEnded {
        transition_name: "sample".to_string(),
        transition_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_scene_transition_ended() {
    let value = sample_scene_transition_ended();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionName").is_some());
    assert!(json.get("transitionUuid").is_some());
    let back: SceneTransitionEnded = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_transition_ended() {
    let value = sample_scene_transition_ended();
    let event = Event::from_parts(
        "SceneTransitionEnded",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SceneTransitionEnded");
}

fn sample_scene_transition_video_ended() -> SceneTransitionVideoEnded {
    SceneTransitionVideoEnded {
        transition_name: "sample".to_string(),
        transition_uuid: "sample".to_string(),
    }
}

#[test]
fn roundtrip_scene_transition_video_ended() {
    let value = sample_scene_transition_video_ended();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("transitionName").is_some());
    assert!(json.get("transitionUuid").is_some());
    let back: SceneTransitionVideoEnded = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_scene_transition_video_ended() {
    let value = sample_scene_transition_video_ended();
    let event = Event::from_parts(
        "SceneTransitionVideoEnded",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "SceneTransitionVideoEnded");
}

fn sample_studio_mode_state_changed() -> StudioModeStateChanged {
    StudioModeStateChanged {
        studio_mode_enabled: false,
    }
}

#[test]
fn roundtrip_studio_mode_state_changed() {
    let value = sample_studio_mode_state_changed();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("studioModeEnabled").is_some());
    let back: StudioModeStateChanged = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_studio_mode_state_changed() {
    let value = sample_studio_mode_state_changed();
    let event = Event::from_parts(
        "StudioModeStateChanged",
        serde_json::to_value(&value).unwrap(),
    )
    .unwrap();
    assert_eq!(event.event_type(), "StudioModeStateChanged");
}

fn sample_screenshot_saved() -> ScreenshotSaved {
    ScreenshotSaved {
        saved_screenshot_path: "sample".to_string(),
    }
}

#[test]
fn roundtrip_screenshot_saved() {
    let value = sample_screenshot_saved();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("savedScreenshotPath").is_some());
    let back: ScreenshotSaved = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_screenshot_saved() {
    let value = sample_screenshot_saved();
    let event =
        Event::from_parts("ScreenshotSaved", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "ScreenshotSaved");
}

fn sample_vendor_event() -> VendorEvent {
    VendorEvent {
        vendor_name: "sample".to_string(),
        event_type: "sample".to_string(),
        event_data: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_vendor_event() {
    let value = sample_vendor_event();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("vendorName").is_some());
    assert!(json.get("eventType").is_some());
    assert!(json.get("eventData").is_some());
    let back: VendorEvent = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_vendor_event() {
    let value = sample_vendor_event();
    let event = Event::from_parts("VendorEvent", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "VendorEvent");
}

fn sample_custom_event() -> CustomEvent {
    CustomEvent {
        event_data: serde_json::Value::Null,
    }
}

#[test]
fn roundtrip_custom_event() {
    let value = sample_custom_event();
    let json = serde_json::to_value(&value).unwrap();
    assert!(json.get("eventData").is_some());
    let back: CustomEvent = serde_json::from_value(json).unwrap();
    assert_eq!(value, back);
}

#[test]
fn event_parts_custom_event() {
    let value = sample_custom_event();
    let event = Event::from_parts("CustomEvent", serde_json::to_value(&value).unwrap()).unwrap();
    assert_eq!(event.event_type(), "CustomEvent");
}
