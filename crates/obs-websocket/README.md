# obs-websocket

Stateful OBS WebSocket v5 client. See the [repository README](../../README.md) for the crate map, protocol version, and examples.

Typed requests and raw requests share one session. Enable `state` to cache scene, input, and output status from events.
